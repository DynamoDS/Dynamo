version: 1.0.{build}

platform: Any CPU

configuration: Release

image: Visual Studio 2017

environment:
  dynamo_solution: src/build.xml


build_script:
  - where msbuild
  - msbuild %dynamo_solution%

  
test: off
