version: 1.0.{build}

platform: Any CPU

configuration: Release

environment:
  dynamo_solution: src/build.xml
  image: Visual Studio 2017

build_script:
  - where msbuild
  - msbuild %dynamo_solution%

  
test: off
