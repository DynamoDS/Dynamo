class A{     a : int = 0;        def foo(x : int)    {       a =        {            x => a1;                        }        return = a ;    }}ax = { A.A(), A.A() };res;[Imperative]{    if(ax[0].a == 0)    {        [Associative]        {            res = ax.foo( 1..2) ;        }    }}
