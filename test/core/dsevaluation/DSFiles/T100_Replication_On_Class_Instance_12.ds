class C{	def f(a : int)	{		return = 1;	} }class D{	static def foo ( t : C[], a, b)    {        return = t.f(a..b);    }}p = {C.C(), C.C()};x = 1;y = 2;a = D.foo(p, x, y); y = 3; 
