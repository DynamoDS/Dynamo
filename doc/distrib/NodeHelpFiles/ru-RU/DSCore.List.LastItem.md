## Подробности
`List.LastItem` возвращает последний элемент из входного списка.

В примере ниже создается список из 10 случайных чисел в диапазоне от 0 до 5. Затем с помощью узла `List.LastItem` возвращается последний элемент списка.
___
## Файл примера

![List.LastItem](./DSCore.List.LastItem_img.jpg)
