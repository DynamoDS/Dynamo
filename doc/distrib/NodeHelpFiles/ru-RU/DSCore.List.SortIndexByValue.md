## Подробности
`List.SortIndexByValue` возвращает список индексов, отсортированных по значениям их элементов в порядке возрастания.

В примере ниже список случайных чисел сортируется, и в качестве значений индекса возвращается новый порядок.
___
## Файл примера

![List.SortIndexByValue](./DSCore.List.SortIndexByValue_img.jpg)
