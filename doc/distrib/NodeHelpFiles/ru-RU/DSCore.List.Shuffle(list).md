## Подробности
`List.Shuffle` случайным образом изменяет порядок элементов списка.

В приведенном ниже примере сначала создается список с помощью `Range`, а затем узел `List.Shuffle` используется для создания списка со случайным порядком.
___
## Файл примера

![List.Shuffle](./DSCore.List.Shuffle(list)_img.jpg)
