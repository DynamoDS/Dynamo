## 詳細
`List.Clean` は、NULL が含まれていないリストと空のリストを返します。

次の例では、`List.Clean` はリストから 2 つの NULL 値を削除し、整数のみを返します。`preserveIndices` 入力はブール値で切り替えることができ、NULL が含まれていたインデックスを保持することができます。
___
## サンプル ファイル

![List.Clean](./DSCore.List.Clean_img.jpg)
