## En detalle:
El nodo `>` es el operador Mayor que. Si la entrada `x` es mayor que la entrada `y`, devuelve "True" (verdadero). Si `x` es menor que `y`, o si los dos valores son iguales, devuelve "False" (falso). En el ejemplo siguiente, se utiliza un nodo `>` para determinar si la entrada `x` es mayor que la entrada `y`. Se utilizan dos controles deslizantes de número para gestionar las entradas del operador `>`.
___
## Archivo de ejemplo

![>](./MK3H4H3ILN766UIQEAC53XJHO5F6H7XDRRBJMXCTVKYDLUFWWIQQ_img.jpg)
