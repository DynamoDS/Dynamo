## 详细
`List.Slice` 根据给定的 `start`、`end` 和 `step` 从输入列表返回子列表。`star` 输入指定输出列表的起始索引。`end` 输入指定其结束索引。此索引不包含在输出列表中。`step` 输入确定分隔列表项的索引数。默认情况下，此输入为 1。

在下面的示例中，使用起始索引 2 和结束索引 5 生成一个新列表，其中包含原始列表中索引为 2、3 和 4 的列表项。
___
## 示例文件

![List.Slice](./DSCore.List.Slice_img.jpg)
