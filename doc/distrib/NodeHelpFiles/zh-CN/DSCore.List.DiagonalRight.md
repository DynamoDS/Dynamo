## 详细
`List.DiagonalRight` 返回一个新列表，其中包含沿列表矩阵中每个对角线(从右下角到左上角)的值列表。

在下面的示例中，我们创建一个点网格，其中直线沿对角线穿过这些点。`List.DiagonalRight` 返回一个点列表，其中包含沿对角线(网格的右下角到左上角)上的点。

___
## 示例文件

![List.DiagonalRight](./DSCore.List.DiagonalRight_img.jpg)
