## 详细
`List.Reorder` 返回一个根据索引列表对其项目重新排序的新列表。

在下面的示例中，通过手动更改索引顺序来创建一个新列表。每个输入索引在列表中为原始列表项指定一个新位置。例如，item0 是 1，因此新列表上的第一项是 2.4，这是原始列表上的索引 1。Item1 是 5，因此新列表上的第二项是 12，这是原始列表上的索引 5，依此类推。
___
## 示例文件

![List.Reorder](./DSCore.List.Reorder_img.jpg)
