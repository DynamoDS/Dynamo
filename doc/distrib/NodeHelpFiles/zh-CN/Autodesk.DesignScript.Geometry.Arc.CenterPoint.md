## 详细
`Arc.CenterPoint` 返回一个位于圆弧中心处的点，该点与相应圆心是同一个点。

在下面的示例中，我们基于 3 个随机生成的点绘制一条圆弧，然后使用 `Arc.CenterPoint` 节点返回其中心点。

___
## 示例文件

![CenterPoint](./Autodesk.DesignScript.Geometry.Arc.CenterPoint_img.jpg)

