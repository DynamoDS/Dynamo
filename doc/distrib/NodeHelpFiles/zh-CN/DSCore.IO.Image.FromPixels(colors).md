## 详细
“From Pixels”将基于输入的二维颜色数组创建一个图像对象。在下例中，我们先使用代码块生成一个从 0 到 255 的数字范围。“Color.ByARGB”节点用于基于此范围创建颜色，并且此节点的连缀设置为“叉积”以创建一个二维数组。然后，我们使用“Image.FromPixels”节点创建一个图像。“Watch Image”节点可用于预览创建的图像。
___
## 示例文件

![FromPixels (colors)](./DSCore.IO.Image.FromPixels(colors)_img.jpg)

