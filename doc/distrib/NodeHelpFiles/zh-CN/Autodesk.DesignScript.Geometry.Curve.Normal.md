## 详细
`Curve.Normal` 返回输入曲线的法线向量。此节点的曲线必须是平面的。

在下面的示例中，我们先使用 `NurbsCurveByControlPoints` 节点(其中一组随机生成的点作为输入)创建 NurbsCurve。我们使用 `Curve.Normal` 节点来查找此曲线的法线。

___
## 示例文件

![Normal](./Autodesk.DesignScript.Geometry.Curve.Normal_img.jpg)

