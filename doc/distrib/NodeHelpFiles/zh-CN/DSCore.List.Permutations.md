## 详细
`List.Permutations` 创建一个由给定输入列表中给定长度的所有排列组成的嵌套列表。

在下面的示例中，我们使用代码块生成从 0 到 5 (步长为 1)的一组数字。然后，我们使用 `List.Permutations` 节点(长度输入为 3)，来生成范围 [0,1,2,3,4,5] 内 3 个元素的所有排列。
___
## 示例文件

![List.Permutations](./DSCore.List.Permutations_img.jpg)
