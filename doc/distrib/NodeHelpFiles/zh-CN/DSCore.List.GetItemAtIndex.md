## 详细
`List.GetItemAtIndex` 获取一个输入列表和一个索引(整数)，并返回列表中给定索引处找到的列表项。

在下面的示例中，我们先使用代码块生成从 0 到 5 (步长为 1)的一组数字。然后，我们使用 `List.GetItemAtIndex` 节点提取索引 4 处的单个元素。
___
## 示例文件

![List.GetItemAtIndex](./DSCore.List.GetItemAtIndex_img.jpg)
