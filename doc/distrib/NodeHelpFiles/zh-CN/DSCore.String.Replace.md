## 详细
“Replace”将搜索给定字符串以查找第二个输入字符串。如果找到该字符串，它将被替换为第三个输入字符串。此节点区分大小写。在下例中，我们以字符串“Hello World”开始。字符串“o”用作要搜索的字符串，而第三个字符串“Hi!”用作要替换为的字符串输入。由于字母“o”在原始字符串中出现两次，因此出现该字母两次的位置都会替换为字符串“Hi!”。
___
## 示例文件

![Replace](./DSCore.String.Replace_img.jpg)

