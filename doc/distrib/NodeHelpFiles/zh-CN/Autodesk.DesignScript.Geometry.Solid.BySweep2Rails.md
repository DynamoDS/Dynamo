## 详细
`Solid.BySweep2Rails` 基于沿两条曲线轨道扫掠的曲线轮廓返回新实体。

在下面的示例中，通过沿两个圆弧(作为轨道)扫掠圆返回一个新实体。在 `path` 和 `guideRail` 中切换圆弧输入将更改扫掠与哪个圆弧垂直。

___
## 示例文件

![BySweep2Rails](./Autodesk.DesignScript.Geometry.Solid.BySweep2Rails_img.jpg)

