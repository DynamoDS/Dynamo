## 详细
`Cuboid.Length` 返回输入立方体的长度。请注意，如果立方体已使用比例因子转换为其他坐标系，则这将返回立方体的原始尺寸，而不是世界空间尺寸。

在该示例中，我们通过角点生成一个立方体，然后使用 `Cuboid.Length` 节点来查找长度。

___
## 示例文件

![Length](./Autodesk.DesignScript.Geometry.Cuboid.Length_img.jpg)

