## 详细
“End Parameter”将返回一个数字，该数字表示可以计算其中输入曲线的域的结束。在大多数情况下，结束参数将为 1。在下例中，我们先使用“ByControlPoints”节点创建一条 Nurbs 曲线，其中一组随机生成的点作为输入。然后，此曲线用作“EndParameter”节点的输入。
___
## 示例文件

![EndParameter](./Autodesk.DesignScript.Geometry.Curve.EndParameter_img.jpg)

