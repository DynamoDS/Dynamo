## 详细
`List.DiagonalLeft` 返回一个新列表，其中包含沿列表矩阵中每个对角线(从左下角到右上角)的元素列表。

在下面的示例中，我们创建一个点网格，其中直线沿对角线穿过这些点。`List.DiagonalLeft` 返回一个点列表，其中包含沿对角线(网格的从左下角到右上角)上的点。


___
## 示例文件

![List.DiagonalLeft](./DSCore.List.DiagonalLeft_img.jpg)
