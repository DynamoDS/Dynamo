## 详细
`List.IsRectangular` 根据一个列表的各行是否有相同数量的元素返回一个布尔值。

在下面的示例中，对于一行中添加了额外点的点网格，将返回一个 False 值。NurbsSurface 可以基于不规则点集进行创建，但其行为可能与预期不符。
___
## 示例文件

![List.IsRectangular](./DSCore.List.IsRectangular_img.jpg)
