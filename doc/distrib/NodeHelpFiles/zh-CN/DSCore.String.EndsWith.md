## 详细
“Ends With”将根据给定字符串是否以第二个输入字符串结尾而返回一个布尔值。默认情况下，此节点区分大小写。布尔值可用于“ignoreCase”输入，以使节点忽略字符串的大小写。在下例中，我们使用字符串“Hello World”作为原始字符串，并使用第二个字符串“orld”作为要搜索的字符串。由于“Hello World”以字符串“orld”结尾，因此“EndsWith”节点会返回“true”。
___
## 示例文件

![EndsWith](./DSCore.String.EndsWith_img.jpg)

