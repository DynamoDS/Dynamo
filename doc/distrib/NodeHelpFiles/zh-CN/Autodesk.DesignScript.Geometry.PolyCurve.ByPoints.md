## 详细
`PolyCurve.ByPoints` 基于一组顶点创建连接曲线。此节点还能够通过 `connectLastToFirst` 输入在闭合形状和开放形状之间切换。

在下面的示例中，沿圆创建点，并使用 `PolyCurve.ByPoints` 节点重绘这些点，以创建闭环多边形形状。

___
## 示例文件

![ByPoints](./Autodesk.DesignScript.Geometry.PolyCurve.ByPoints_img.jpg)

