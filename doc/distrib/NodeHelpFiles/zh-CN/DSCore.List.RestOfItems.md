## 详细
`List.RestOfItems` 从列表中删除第一个元素，并在新列表中返回其余元素。

在下面的示例中，我们从 0 到 5 (步长为 1)的一组数字开始。通过使用 `List.RestOfItems` 节点，我们删除列表中的第一项，并输出一个包含数字 1 到 5 的列表。
___
## 示例文件

![List.RestOfItems](./DSCore.List.RestOfItems_img.jpg)
