## 详细
`List.Cycle` 获取一个输入列表，并返回一个新列表，该新列表由对输入列表进行 `amount` 输入指定的重复次数后生成的结果列表组成。

在下面的示例中，我们先使用代码块生成从 0 到 5 (步长为 1)的一组数字。然后，我们使用输入数量为 3 的 `List.Cycle` 节点。结果是一个包含 18 个元素的列表，由对原始列表的 3 个循环组成。
___
## 示例文件

![List.Cycle](./DSCore.List.Cycle_img.jpg)
