## 详细
`List.Sort` 根据数据类型的内置排序来组织项目列表。例如，数字列表按从小到大的顺序排列，而字符串列表按字母顺序排列。

在下面的示例中，我们使用 `Math.RandomList` 来先生成一列介于 0 和 1 之间的随机数。然后，我们乘以 10，并使用 `Math.Floor` 操作来获取一列介于 0 和 9 之间的随机整数。然后，`List.Sort` 创建一个数字从最小到最大的排序列表。
___
## 示例文件

![List.Sort](./DSCore.List.Sort_img.jpg)
