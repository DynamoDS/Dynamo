## 详细
`List.AllIndicesOf` 在给定列表中搜索给定项，并返回找到该项的索引列表。如果未找到任何项，`List.AllIndicesOf` 返回一个空列表。

在下面的示例中，我们先生成一个字符串列表，每个字符串都包含 A 和 D 之间的一个字母。然后，我们使用 `List.AllIndicesOf` 在列表中搜索字符串“B”，该字符串位于索引 1 和 4 处。
___
## 示例文件

![List.AllIndicesOf](./DSCore.List.AllIndicesOf_img.jpg)
