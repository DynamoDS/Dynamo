## 详细
`List.IsHomogeneous` 根据列表中元素是否都是同一类型返回一个布尔值。

在下面的示例中，将一个简单的数字列表与插入有字符串的同一个列表进行比较。原始列表是同类(仅数字)，因此 `List.IsHomogeneous` 返回 True。新列表不是同类(数字和字符串)，因此 `List.IsHomogeneous` 返回 False。
___
## 示例文件

![List.IsHomogeneous](./DSCore.List.IsHomogeneous_img.jpg)
