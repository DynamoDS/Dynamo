## 详细
`List.ShiftIndices` 将列表中各项的位置按 `amount` 输入进行移位。`amount` 输入中的正数将数字向上移位，而负数将索引向后移位。各项环绕，从而使列表后面的各项绕到开头。

在下面的示例中，我们先使用 `Range` 生成一个列表，然后将索引向前移动 3 个位置。原始列表中的最后 3 个数字环绕以成为新列表中的前 3 个数字。
___
## 示例文件

![List.ShiftIndices](./DSCore.List.ShiftIndices_img.jpg)
