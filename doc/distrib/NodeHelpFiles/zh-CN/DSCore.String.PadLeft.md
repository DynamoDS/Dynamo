## 详细
“PadLeft”将根据“newWidth”输入增加输入字符串的宽度，方法是在原始字符串的左侧添加“padChars”输入。如果原始字符串长于“newWidth”输入，则此节点会原样返回原始字符串。在下例中，我们使用字符串“Hello World”作为输入字符串。带下划线的字符串用作填充字符，同时使用一个“整数”滑块来控制字符串的新宽度。
___
## 示例文件

![PadLeft](./DSCore.String.PadLeft_img.jpg)

