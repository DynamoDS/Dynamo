## 详细
`Point.Add` 将输入向量的 x、y 和 z 分量添加到输入点的相应坐标。这与通过给定向量平移点相同，相当于使用 `Geometry.Translate (vector)`。

在下面的示例中，我们使用代码块指定 x、y 和 z 坐标以创建一个点，然后添加世界 XAxis 向量作为 `vectorToAdd`。结果是点在 X 轴正方向上平移 1 个单位距离。

___
## 示例文件

![Add](./Autodesk.DesignScript.Geometry.Point.Add_img.jpg)

