## 详细
`List.ReplaceItemAtIndex` 替换输入列表中给定索引处的项。

在下面的示例中，我们先从一列 0 到 5 的数字开始。然后，我们使用 `List.ReplaceItemAtIndex` 节点以将索引 3 处的项替换为新项，在本例中为整数 10。
___
## 示例文件

![List.ReplaceItemAtIndex](./DSCore.List.ReplaceItemAtIndex_img.jpg)
