## 详细
`==` 节点是“等于”运算符。如果 `x` 输入等于 `y` 输入，则返回 True。如果两个值不相等，则返回 False。此节点可用于比较数字和非数字(例如，字符串、Revit 对象、几何图形等)。

在下面的示例中，`==` 节点用于确定 x 和 y 输入是否相等。我们使用两个数字滑块来控制 `==` 运算符的输入。
___
## 示例文件

![==](./==_img.jpg)
