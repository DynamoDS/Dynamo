## 详细
“Segment Length At Parameter”将返回曲线在起点和输入参数之间的长度，其中 0 是曲线起点处的参数，1 是曲线终点处的参数。在下例中，我们使用两组随机数来生成点列表。这些点用于通过控制点创建 Nurbs 曲线。一个设置为范围 0 到 1 的“数字”滑块用于“SegmentLengthAtParameter”节点中的参数输入。
___
## 示例文件

![SegmentLengthAtParameter](./Autodesk.DesignScript.Geometry.Curve.SegmentLengthAtParameter_img.jpg)

