## 详细
`List.Join` 将输入列表合并为一个列表。可以使用“Join”节点上的“+”和“-”按钮来调整输入列表的数量。

在下面的示例中，我们先使用两个代码块生成两组数字。第一个列表是奇数 [1,3,5]，第二个列表是偶数 [0,2,4]。使用 `List.Join` 节点，我们可以将这两个列表连接为一个列表 [1,3,5,0,2,4]。

___
## 示例文件

![List.Join](./DSCore.List.Join_img.jpg)
