## 详细
“Mesh.Translate (vector,distance)”节点在给定的“direction”上按给定的“distance”平移网格。

在下面的示例中，一个简单的网格在向量定义的方向上平移，并用一条由两个点构造的线表示。“Line.ByStartPointEndPoint”节点用于可视化平移的方向。

## 示例文件

![Example](./Autodesk.DesignScript.Geometry.Mesh.Translate(mesh,%20vector,%20distance)_img.jpg)
