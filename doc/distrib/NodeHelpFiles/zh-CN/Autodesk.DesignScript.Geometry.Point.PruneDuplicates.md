## 详细
“Prune Duplicates”会将点列表作为输入，并返回一个重复点已删除的列表。公差输入用于确定需要将两个点视为重复点的接近距离。如果两个点的接近距离小于公差，将保留列表中的第一个点，并删除另一个点。在本例中，我们生成一组随机点。然后，我们使用公差为 1 的“Prune Duplicates”，来删除与另一个点的接近距离小于 1 单位的任何点。
___
## 示例文件

![PruneDuplicates](./Autodesk.DesignScript.Geometry.Point.PruneDuplicates_img.jpg)

