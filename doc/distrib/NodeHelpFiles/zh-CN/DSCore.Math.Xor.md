## 详细
`Math.Xor` 将接受两个布尔值或两个布尔数值列表，并确定这些值是相同还是不同。`Math.Xor` 将比较输入值，如果其中一个输入值恰好为 True，则返回 True。如果两个值都为 True 或都为 False，则 `Math.Xor` 返回 False。

在下面的示例中，我们将从两个布尔值列表开始。这些列表一起表示 True 和 False 值的 4 种可能组合。请注意，与 `And` 和 `Or` 节点不同，`Math.Xor` 只允许两个值。
___
## 示例文件

![Math.Xor](./DSCore.Math.Xor_img.jpg)
