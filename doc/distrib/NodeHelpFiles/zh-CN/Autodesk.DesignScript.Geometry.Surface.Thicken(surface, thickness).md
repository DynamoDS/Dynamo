## 详细
`Surface.Thicken` 通过根据厚度输入偏移曲面并对端口封口来闭合实体，以创建一个实体。

在下面的示例中，我们先使用 `Surface.BySweep2Rails` 创建一个曲面。然后，我们通过使用一个数字滑块来确定 `Surface.Thicken` 节点的厚度输入以创建一个实体。


___
## 示例文件

![Surface.Thicken](./Autodesk.DesignScript.Geometry.Surface.Thicken(surface,%20thickness)_img.jpg)
