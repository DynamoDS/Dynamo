## 详细
“Last Index Of”将搜索给定字符串以查找第二个输入字符串。如果找到第二个字符串，则此节点会返回最后一次出现时的索引。默认情况下，此节点区分大小写。布尔值可用于“ignoreCase”输入，以使节点忽略字符串的大小写。在下例中，我们使用字符串“Hello World”，并搜索字符串“o”。在基础字符串中找到该字符串两次，并返回该字符串最后一次出现时的索引。
___
## 示例文件

![LastIndexOf](./DSCore.String.LastIndexOf_img.jpg)

