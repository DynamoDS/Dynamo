## 详细
“Math.RandomList(amount,value1,value2,seed)”将生成一个具有给定数量且介于两个输入值之间的随机列表。此节点包含名为“seed”的输入。种子是一个值，用于启动随机化函数(允许为每个种子输入提供其他选项)，并允许通过使用相同的种子值来“recall”随机值。

___
## 示例文件

![Math.RandomList](./DSCore.Math.RandomList%28amount%2C%20value1%2C%20value2%2C%20seed%29_img.png)
