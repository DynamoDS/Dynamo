## 详细
“RemapRange”将获取一个数字列表，并将这些数字线性缩放到一个新的指定范围。原始列表的最小值将映射到“newMin”输入，而原始列表的最大值将映射到“newMax”输入。其余数字将进行缩放，以保持分布比。在下例中，我们使用“RandomList”节点，来生成一个要重新映射的数字列表。我们使用两个“数字”滑块来控制结果列表的新最小值和最大值。
___
## 示例文件

![RemapRange](./DSCore.Math.RemapRange_img.jpg)

