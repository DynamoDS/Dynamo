## 详细
`PolyCurve.Curves` 返回组成输入 PolyCurve 的各条曲线的列表。

在下面的示例中，我们生成一组随机点，然后使用 `PolyCurve.ByPoints` 以生成开放 PolyCurve。然后，我们使用 `PolyCurve.Curves` 以生成组成 PolyCurve 的各条曲线的列表。

___
## 示例文件

![Curves](./Autodesk.DesignScript.Geometry.PolyCurve.Curves_img.jpg)

