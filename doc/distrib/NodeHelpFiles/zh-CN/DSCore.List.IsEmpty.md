## 详细
`List.IsEmpty` 输入列表为空时返回布尔值 True；如果输入列表不为空，则返回 False。

在下面的示例中，我们先使用代码块生成从 0 到 5 (步长为 1)的一组数字。我们将此列表输入到 `List.IsEmpty` 节点中。由于该列表不为空，因此 `List.IsEmpty` 返回 False 值。
___
## 示例文件

![List.IsEmpty](./DSCore.List.IsEmpty_img.jpg)
