## 详细
在下面的示例中，使用 `TSplineSurface.ByPlaneOriginNormal` 创建平面 T-Spline 曲面，并选择和细分其一组面。然后，使用 `TSplineSurface.ExtrudeFaces` 节点、给定方向(在本例中为面的 UVN 法线向量)和数个跨度对称拉伸这些面。生成的边将在指定方向上进行位移。
___
## 示例文件

![TSplineSurface.ExtrudeFaces](./Autodesk.DesignScript.Geometry.TSpline.TSplineSurface.ExtrudeFaces_img.jpg)
