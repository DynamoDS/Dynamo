## 详细
`List.SortByKey` 接收要排序的输入列表和一列相应可排序键(如数字或字符串)。`List.SortByKey` 按升序对键列表进行排序，并重新排列输入列表以与已排序键的新顺序相对应。此节点输出已重新排列的列表和已排序键列表。

在下面的示例中，我们使用从 0 到 9 的范围作为输入列表。对于键列表，我们生成一列 10 个介于 0 和 4 之间的随机数。键列表按数字排序，而从 0 到 9 的范围根据键列表中相应元素的顺序重新排列。
___
## 示例文件

![List.SortByKey](./DSCore.List.SortByKey_img.jpg)
