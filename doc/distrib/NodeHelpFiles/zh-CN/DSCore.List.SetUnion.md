## 详细
`List.SetUnion` 通过合并两个输入列表的对象并消除重复值来返回一个新列表。

在下面的示例中，将一个列表(从 0 到 9，步长为 1)与另一个列表(从 0 到 18，步长为 2)进行比较。生成的列表包含 List1 和 List2 中的所有元素(并消除 0、2、4 等重复项)。
___
## 示例文件

![List.SetUnion](./DSCore.List.SetUnion_img.jpg)
