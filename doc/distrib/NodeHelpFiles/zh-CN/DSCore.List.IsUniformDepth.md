## 详细
`List.IsUniformDepth` 根据列表的深度是否一致(这意味着每个列表内都嵌套有相同数量的列表)返回一个布尔值。

在下面的列表中，将两个列表进行比较(一个是均匀深度，另一个是非均匀深度)以显示差异。均匀列表包含三个列表，没有嵌套列表。非均匀列表包含两个列表。第一个列表没有嵌套列表，但第二个列表有两个嵌套列表。列表 [0] 和 [1] 处的深度不相等，因此 `List.IsUniformDepth` 返回 False。
___
## 示例文件

![List.IsUniformDepth](./DSCore.List.IsUniformDepth_img.jpg)
