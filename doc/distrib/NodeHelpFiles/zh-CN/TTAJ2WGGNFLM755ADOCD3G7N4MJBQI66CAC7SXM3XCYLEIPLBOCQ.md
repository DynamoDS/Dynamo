<!--- Autodesk.DesignScript.Geometry.TSpline.TSplineSurface.ByTorusCoordinateSystemRadii --->
<!--- TTAJ2WGGNFLM755ADOCD3G7N4MJBQI66CAC7SXM3XCYLEIPLBOCQ --->
## In-Depth
在下面的示例中，使用位于给定坐标系 `cs` 处的原点创建 T-Spline 圆环体曲面。形状的小半径和大半径由 `innerRadius` 和 `outerRadius` 输入设置。`innerRadiusSpans` 和 `outerRadiusSpans` 的值控制曲面沿两个方向的定义。形状的初始对称由 `symmetry` 输入指定。如果已应用于形状的轴向对称在 X 轴或 Y 轴上处于活动状态，则圆环体的 `outerRadiusSpans` 值必须是 4 的倍数。径向对称没有此类要求。最后，`inSmoothMode` 输入用于在 T-Spline 曲面的平滑模式和长方体模式预览之间切换。

## 示例文件

![Example](./TTAJ2WGGNFLM755ADOCD3G7N4MJBQI66CAC7SXM3XCYLEIPLBOCQ_img.jpg)
