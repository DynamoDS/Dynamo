## 详细
`List.Flatten` 从多维列表(至少有一个嵌套列表的列表)返回一维列表(只有一个级别的列表)。

在下面的示例中，通过在每个网格的指定索引处创建一个球体来比较点网格的两个列表结构。展平的点列表返回索引位置处的 1 个点。未展平的列表返回索引位置处的 10 个点，因为每个索引都包含一行点。
___
## 示例文件

![List.Flatten](./DSCore.List.Flatten_img.jpg)
