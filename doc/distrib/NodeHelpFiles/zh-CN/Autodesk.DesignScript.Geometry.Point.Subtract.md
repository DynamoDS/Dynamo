## 详细
`Point.Subtract` 从输入点的相应坐标中减去输入向量的 x、y 和 z 分量。这与在给定向量的反方向上平移点相同。

在下面的示例中，我们使用代码块指定 x、y 和 z 坐标以创建一个点，然后添加世界 XAxis 向量作为 `vectorToSubtract`。结果是点在 X 轴负方向上平移 1 个单位距离。

___
## 示例文件

![Subtract](./Autodesk.DesignScript.Geometry.Point.Subtract_img.jpg)

