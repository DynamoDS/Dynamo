## 详细
`Curve.Length` 返回输入曲线的长度。

在下面的示例中，我们先使用 `NurbsCurve.ByControlPoints` 节点(其中一组随机生成的点作为输入)创建 NurbsCurve。我们可以使用 `Curve.Length` 节点来查找此曲线的长度。

___
## 示例文件

![Length](./Autodesk.DesignScript.Geometry.Curve.Length_img.jpg)

