## 详细
`List.SetIntersection` 返回一个新列表，其中仅包含同时包含在输入列表中的对象。

在下面的示例中，`List.SetIntersection` 将一个列表(从 0 到 9，步长为 1)与另一个列表(从 0 到 18，步长为 2)进行比较。返回小于 10 的偶数，因为这些偶数同时包含在 List1 和 List2 中。
___
## 示例文件

![List.SetIntersection](./DSCore.List.SetIntersection_img.jpg)
