## 详细
“Dimensions”将返回输入图像的宽度和高度(以像素为单位)。在下例中，我们先使用“File Path”节点和“File.FromPath”，来创建一个指向图像的文件对象。然后，使用“Image.ReadFromFile”来基于文件创建一个图像对象。“Dimensions”节点显示此图像的宽度和高度分别为 270 像素。
___
## 示例文件

![Dimensions](./DSCore.IO.Image.Dimensions_img.jpg)

