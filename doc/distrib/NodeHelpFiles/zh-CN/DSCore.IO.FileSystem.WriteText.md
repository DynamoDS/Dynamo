## 详细
“WriteText”会将字符串写入到指定文件。如果文件不存在，此节点将创建该文件。要在输出文件中创建换行符，我们可以使用转义符“\r\n”。在下例中，我们有一个字符串列表，想要将其在文本文件中作为单独三行写入。我们使用“\r\n”作为分隔符将列表连接到单个字符串中。然后，我们使用“WriteText”节点，将其写入到文本文件中。
___
## 示例文件

![WriteText](./DSCore.IO.FileSystem.WriteText_img.jpg)

