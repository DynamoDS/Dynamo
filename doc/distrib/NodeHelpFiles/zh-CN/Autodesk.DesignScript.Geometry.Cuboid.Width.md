## 详细
`Cuboid.Width` 返回输入立方体的宽度。

在下面的示例中，我们通过角点生成一个立方体，然后使用 `Cuboid.Width` 节点来查找其宽度。请注意，如果立方体已使用比例因子转换为其他坐标系，则这将返回立方体的原始尺寸，而不是世界空间尺寸。

___
## 示例文件

![Width](./Autodesk.DesignScript.Geometry.Cuboid.Width_img.jpg)

