## 详细
`List.GroupByKey` 根据一列键对输入列表中的各项进行分组。键列表的元素数量应与输入列表的元素数量相同。原始列表中与匹配键对应的项目在输出列表中分组在一起。

在下面的示例中，我们使用一组介于 0 和 9 之间的数字作为输入列表。我们还生成一列 10 个介于 0 和 4 之间的数字以用作键。`List.GroupByKey` 输出一个元素嵌套列表(根据相应键进行分组)。它还输出一列唯一键。
___
## 示例文件

![List.GroupByKey](./DSCore.List.GroupByKey_img.jpg)
