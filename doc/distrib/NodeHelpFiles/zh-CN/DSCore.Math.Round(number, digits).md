## 详细
“Round with digits”将允许用户选择要将输入数舍入到的小数位数。输入零将舍入到最接近的整数。不允许值小于零。在下例中，我们使用“Rand”节点生成随机数。使用“整数”滑块来控制“Round”节点中的“digits”输入。
___
## 示例文件

![Round (number, digits)](./DSCore.Math.Round(number,%20digits)_img.jpg)

