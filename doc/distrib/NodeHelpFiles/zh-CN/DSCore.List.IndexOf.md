## 详细
`List.IndexOf` 返回列表中给定元素的索引位置。如果给定元素在列表中找不到或其数据类型与列表元素不匹配(例如，输入为 double，而列表元素为 string)，则 `List.IndexOf` 返回空值。

请注意，此节点仅返回第一个索引，即使元素在列表中出现多次也是如此。要返回多个索引，请改用 `List.AllIndicesOf`。
___
## 示例文件

![List.IndexOf](./DSCore.List.IndexOf_img.jpg)
