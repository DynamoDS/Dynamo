## 详细
`List.TakeItems` 从输入列表开头返回给定数量的列表项。如果 `amount` 输入为负数，则从列表末尾以倒序获取列表项。

在下面的示例中，我们从 0 到 9 (步长为 1)的一组数字开始。我们使用整数滑块作为 `amount` 输入，以指定要从原始列表中获取的元素数量。当输入 4 时，输出列表包含原始列表中的前 4 个元素。
___
## 示例文件

![List.TakeItems](./DSCore.List.TakeItems_img.jpg)
