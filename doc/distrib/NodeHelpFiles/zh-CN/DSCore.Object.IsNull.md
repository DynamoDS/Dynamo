## 详细
“IsNull”将根据对象是否为空值而返回一个布尔值。在下例中，将根据位图中的“红色”级别，以不同的半径绘制圆网格。如果没有“红色”值，则不会绘制圆，并在圆列表中返回空值。通过“IsNull”传递此列表，将返回布尔值列表，其中 true 表示空值的每个位置。此布尔列表可与“List.FilterByBoolMask”一起使用，来返回不带空值的列表。
___
## 示例文件

![IsNull](./DSCore.Object.IsNull_img.jpg)

