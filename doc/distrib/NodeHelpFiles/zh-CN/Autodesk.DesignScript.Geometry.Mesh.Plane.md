## 详细
“Mesh.Plane”使用输入点作为原点、给定的 x、y 方向宽度和给定的 x、y 分段创建一个扁平矩形网格。如果将 x 或 y 细分数设置为 0，则任一输入的默认值都将生成 5。

在下面的示例中，“Mesh.Plane”用于创建一个平面网格，其中 4 条栅格线沿 X 轴，8 条栅格线沿 Y 轴。“Mesh.Triangles”节点用于可视化网格三角形的分布。

## 示例文件

![Example](./Autodesk.DesignScript.Geometry.Mesh.Plane_img.jpg)
