## 详细
`List.TrueForAny`返回一个布尔值，以表明 `queryFunction` 输入中的条件对于列表中的至少一个列表项是否为 True。`queryFunction` 输入必须是一个布尔值。

下面的示例显示了两个用于计算输入列表的函数。第一个函数使用大于(>)节点计算任何列表值是否大于 11，将返回 False。第二个函数使用小于(<)节点计算任何列表值是否小于 5，将返回 True。
___
## 示例文件

![List.TrueForAny](./List.TrueForAny_img.jpg)
