## 详细
`List.Count` 返回输入列表中的项数。

在下面的示例中，我们创建一个点网格。然后，我们比较列表展平前后的列表项计数。展平前，`List.Count` 返回列表中的 11 项(子列表)。展平后，Count 返回 121 项，因为所有子列表均已删除，点位于同一列表级别。
___
## 示例文件

![List.Count](./DSCore.List.Count_img.jpg)
