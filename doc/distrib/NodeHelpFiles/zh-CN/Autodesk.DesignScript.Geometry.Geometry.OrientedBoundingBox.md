## 详细
`Geometry.OrientedBoundingBox` 返回由其包含的几何图形定义的最小体积方向框。BoundingBox 不会显示，但可以使用 `BoundingBox.ToPolySurface` 节点将其返回为 PolySurface。

在下面的示例中，为未合并的实体创建 BoundingBox，并将其表示为 PolySurface。
___
## 示例文件

![Geometry.OrientedBoundingBox](./Autodesk.DesignScript.Geometry.Geometry.OrientedBoundingBox_img.jpg)
