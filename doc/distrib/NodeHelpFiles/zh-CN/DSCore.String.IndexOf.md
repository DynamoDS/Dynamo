## 详细
“IndexOf”将搜索给定字符串以查找第二个输入字符串。如果找到第二个字符串，则此节点会返回该字符串第一次出现时其第一个字符的索引。默认情况下，此节点区分大小写。布尔值可用于“ignoreCase”输入，以使节点忽略字符串的大小写。在下例中，我们使用字符串“Hello World”，并搜索字符“o”；此字母在字符串中出现两次，但“IndexOf”节点仅返回该字母第一次出现时的索引。
___
## 示例文件

![IndexOf](./DSCore.String.IndexOf_img.jpg)

