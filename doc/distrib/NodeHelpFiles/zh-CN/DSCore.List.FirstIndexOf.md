## 详细
`List.FirstIndexOf` 在给定列表中搜索以查找 `Item` 输入中指定的某项，并返回该项在列表中第一个实例的索引。如果在列表中找不到该项，则节点返回 `-1`。

在下面的示例中，我们先生成一个包含 10 个介于 0 到 4 之间随机数的列表。通过使用 `List.FirstIndexOf node` 节点，我们搜索特定项，并检索在列表中首次找到该项的索引。
___
## 示例文件

![List.FirstIndexOf](./DSCore.List.FirstIndexOf_img.jpg)
