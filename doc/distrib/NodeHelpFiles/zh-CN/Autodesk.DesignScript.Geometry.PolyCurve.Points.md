## 详细
`PolyCurve.Points` 返回第一分量曲线的起点和所有其它分量曲线的终点。它不会返回闭合 PolyCurves 的重复点。

在下面的示例中，`Polygon.RegularPolygon` 分解为曲线列表，然后重新连接为 PolyCurve。然后，使用 `PolyCurve.Points` 返回 PolyCurve 的点。
___
## 示例文件

![PolyCurve.Points](./Autodesk.DesignScript.Geometry.PolyCurve.Points_img.jpg)
