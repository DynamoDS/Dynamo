## 深入資訊
`PolySurface.Chamfer` 會傳回一個倒角邊由 `offset` 輸入定義的新 PolySurface。

在以下範例中，透過將圓柱的頂邊和底邊倒角 0.5 的偏移來傳回新的 PolySurface。offset 輸入必須足夠小才能落在 PolySurface 上。此節點與 `PolySurface.Fillet` 類似，但會傳回有新邊的 PolySurface。

___
## 範例檔案

![PolySurface.Chamfer](./Autodesk.DesignScript.Geometry.PolySurface.Chamfer_img.jpg)
