## 深入資訊
`List.RestOfItems` 會從清單中移除第一個元素，然後以新清單傳回剩餘元素。

在以下範例中，我們先產生一個從 0 到 5，以 1 遞增的數字範圍。使用 `List.RestOfItems` 節點，我們移除清單中的第一個項目，輸出是一個 1 到 5 的數字清單。
___
## 範例檔案

![List.RestOfItems](./DSCore.List.RestOfItems_img.jpg)
