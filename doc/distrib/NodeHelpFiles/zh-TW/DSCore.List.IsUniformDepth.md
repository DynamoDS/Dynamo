## 深入資訊
`List.IsUniformDepth` 會根據清單的深度是否一致傳回布林值，這表示每個清單中巢狀的清單數目都相同。

以下範例比較兩個清單，一個深度一致，另一個深度不一致，以顯示差異。一致的清單包含三個沒有巢狀清單的清單。不一致的清單包含兩個清單: 第一個清單沒有巢狀清單，第二個清單則有兩個巢狀清單。[0] 和 [1] 的清單深度不同，因此 `List.IsUniformDepth` 傳回 False。
___
## 範例檔案

![List.IsUniformDepth](./DSCore.List.IsUniformDepth_img.jpg)
