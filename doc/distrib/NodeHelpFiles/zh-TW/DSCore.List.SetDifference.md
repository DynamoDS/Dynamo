## 深入資訊
`List.SetDifference` 會傳回一個包含在 list1 中而不在 list2 中的物件的新清單。

在以下範例中，將一個 0 到 9 (以 1 遞增) 的清單與一個 0 到 18 (以 2 遞增) 的清單進行比較。`List.SetDifference` 會傳回小於 10 的奇數，因為這些數只在 list1 中而不在 list2 中。
___
## 範例檔案

![List.SetDifference](./DSCore.List.SetDifference_img.jpg)
