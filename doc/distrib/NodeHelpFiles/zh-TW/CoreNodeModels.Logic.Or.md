## 深入資訊
Or 接受任意的布林輸入數。您可以使用 Or 節點上的「+」和「-」按鈕增加或減少輸入數。每個輸入都必須是單一布林值。如果至少一個輸入為 true，Or 會傳回「true」，如果所有輸入都為「false」，則會傳回「false」。在以下範例中，我們有三個布林切換作為 Or 節點的輸入。當全部三個都設定為「false」時，Or 會傳回「false」。
___
## 範例檔案

![Or](./CoreNodeModels.Logic.Or_img.jpg)

