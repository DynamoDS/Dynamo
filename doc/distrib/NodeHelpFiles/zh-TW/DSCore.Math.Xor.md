## 深入資訊
`Math.Xor` 接受兩個布林值或兩個布林值清單，並判斷這兩個值是否相同。`Math.Xor` 會比較輸入值，如果只有一個輸入為 True，則傳回 True。如果兩個值皆為 True，或兩個值皆為 False，`Math.Xor` 會傳回 False。

在以下範例中，我們從兩個布林值清單開始。這兩個清單搭配會有 True 和 False 值的 4 種可能組合。請注意，`Math.Xor` 與 `And` 和 `Or` 不同，只允許兩個值。
___
## 範例檔案

![Math.Xor](./DSCore.Math.Xor_img.jpg)
