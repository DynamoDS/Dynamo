## 深入資訊
RemapRange 接受數字清單，然後將這些數字以線性調整比例到新的指定範圍。原始清單的最小值對映到 newMin 輸入，原始清單的最大值對映到 newMax 輸入。剩餘的數字會跟著調整比例以維持分佈比率。在以下範例中，我們使用 RandomList 節點產生一個要重新對映的數字清單。我們使用兩個數字滑棒控制結果清單新的最小值和最大值。
___
## 範例檔案

![RemapRange](./DSCore.Math.RemapRange_img.jpg)

