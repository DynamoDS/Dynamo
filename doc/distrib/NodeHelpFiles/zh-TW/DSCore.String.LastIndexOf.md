## 深入資訊
LastIndexOf 會搜尋給定字串是否有第二個輸入字串。如果找到第二個字串，此節點會傳回最後一次出現的索引。此節點預設區分大小寫。「ignoreCase」輸入可以使用布林值，讓節點忽略字串的大小寫。在以下範例中，我們使用字串「Hello World」，並搜尋字串「o」。此字串在基底字串中出現兩次，因此傳回字串最後一次出現的索引。
___
## 範例檔案

![LastIndexOf](./DSCore.String.LastIndexOf_img.jpg)

