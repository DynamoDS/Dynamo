## 深入資訊
`List.ReplaceItemAtIndex` 會取代輸入清單中給定索引的項目。

在以下範例中，我們先產生一個 0 到 5 的數字範圍，然後使用 `List.ReplaceItemAtIndex` 節點，將索引 3 的項目取代為新項目，在此範例中為整數 10。
___
## 範例檔案

![List.ReplaceItemAtIndex](./DSCore.List.ReplaceItemAtIndex_img.jpg)
