## 深入資訊
WriteText 會將字串寫入指定的檔案。如果該檔案不存在，此節點會建立檔案。若要在輸出檔案中建立新行，我們可以使用逸出字元「\r\n」。在以下範例中，我們有一個字串清單，想要在文字檔中分別寫入這三行。我們使用「\r\n」作為分隔符號將清單接合成單一字串，然後使用 WriteText 節點將該字串寫入文字檔。
___
## 範例檔案

![WriteText](./DSCore.IO.FileSystem.WriteText_img.jpg)

