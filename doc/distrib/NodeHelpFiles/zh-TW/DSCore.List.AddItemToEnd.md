## 深入資訊
`List.AddItemtoEnd` 會將給定項目插入給定清單的結尾。請注意，如果將清單物件當作要加入的項目，則會當作單一物件加入而產生巢狀清單。若要將兩個清單合併成單一展開清單，請參閱 `List.Join`。

在以下範例中，我們使用 Code Block 產生一個從 0 到 5，以 1 遞增的數字範圍，然後使用 `List.AddItemToEnd` 將新項目 (數字 20) 加到該清單的結尾。
___
## 範例檔案

![List.AddItemToEnd](./DSCore.List.AddItemToEnd_img.jpg)
