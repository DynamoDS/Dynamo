## 深入資訊
`List.GetItemAtIndex` 接受輸入清單和索引 (整數)，並傳回在清單中給定索引處找到的項目。

在以下範例中，我們先使用 Code Block 產生一個從 0 到 5，以 1 遞增的數字範圍，然後使用 `List.GetItemAtIndex` 節點擷取索引 4 的單一元素。
___
## 範例檔案

![List.GetItemAtIndex](./DSCore.List.GetItemAtIndex_img.jpg)
