## 深入資訊
Dimensions 會傳回輸入影像的寬度和高度 (以像素為單位)。在以下範例中，我們先使用 File Path 節點和 File.FromPath 建立一個指向影像的檔案物件。然後使用 Image.ReadFromFile 根據檔案建立影像物件。Dimensions 節點顯示此影像的寬度和高度為 270 像素。
___
## 範例檔案

![Dimensions](./DSCore.IO.Image.Dimensions_img.jpg)

