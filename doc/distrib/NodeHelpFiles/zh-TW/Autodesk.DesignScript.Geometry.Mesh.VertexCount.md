## 深入資訊
此節點會計算提供網格中的頂點數。在以下範例中，使用 `Mesh.Cuboid` 建立立方體網格，然後使用該網格作為輸入計算頂點數。除此之外，還使用 `Mesh.Vertices` 亮顯立方體的頂點，方便獲得更好的預覽效果，並檢視每個頂點的座標清單。

## 範例檔案

![Example](./Autodesk.DesignScript.Geometry.Mesh.VertexCount_img.jpg)
