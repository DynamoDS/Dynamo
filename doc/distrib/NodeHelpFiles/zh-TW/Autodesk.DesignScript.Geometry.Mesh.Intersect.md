## 深入資訊
`Mesh.Intersect` 節點會將網格與輸入平面相交，傳回一條或多條 PolyCurve。

以下範例在兔子形狀的網格上使用 `Mesh.Intersect`。網格會與一系列輸入平面相交，產生許多切片作為輪廓。這是準備模型以在雷射切割機、水刀切割機或 CNC 銑床上製造的起點。

## 範例檔案

![Example](./Autodesk.DesignScript.Geometry.Mesh.Intersect_img.jpg)
