## 深入資訊
`Polygon.ByPoints` 從輸入點清單建立多邊形。這些點將依其來源清單的順序連接。

在以下範例中，我們建立一個由數字滑棒驅動的隨機點清單，然後使用 `Polygon.ByPoints` 節點將各點連成線。

___
## 範例檔案

![ByPoints](./Autodesk.DesignScript.Geometry.Polygon.ByPoints_img.jpg)

