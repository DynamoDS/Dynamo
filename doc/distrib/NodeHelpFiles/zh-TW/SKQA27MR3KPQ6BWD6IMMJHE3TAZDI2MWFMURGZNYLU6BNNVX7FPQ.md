## 深入資訊
`>=` 節點是「大於或等於」運算子。如果 `x` 輸入大於 `y` 輸入，或兩個值相等，存在於 1.x 字典中；編輯的圖表名稱: GreaterThanOrEqualTo.dyn Y

它會傳回 True。如果 `x` 小於 `y`，則此運算子會傳回 False。在以下範例中，使用 `>=` 節點判斷 `x` 輸入是否大於或等於 `y` 輸入。我們使用兩個數字滑棒控制 `>=` 運算子的輸入。
___
## 範例檔案

![>=](./SKQA27MR3KPQ6BWD6IMMJHE3TAZDI2MWFMURGZNYLU6BNNVX7FPQ_img.jpg)
