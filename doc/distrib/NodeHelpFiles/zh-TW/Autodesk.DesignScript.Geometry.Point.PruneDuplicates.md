## 深入資訊
PruneDuplicates 以點清單當作輸入，並傳回已移除重複點的清單。使用 tolerance 輸入決定將兩個點視為重複的距離。如果兩個點比公差近，則保留清單中的第一點，而移除另一點。在範例中，我們產生一組隨機點，然後使用 PruneDuplicates 和公差 1，以移除距離另一點比一個單位近的所有點。
___
## 範例檔案

![PruneDuplicates](./Autodesk.DesignScript.Geometry.Point.PruneDuplicates_img.jpg)

