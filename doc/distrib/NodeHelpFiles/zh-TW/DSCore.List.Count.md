## 深入資訊
`List.Count` 會傳回輸入清單中的項目數。

在以下範例中，我們建立點格線，然後比較清單展開前後的清單項目計數。展開前，`List.Count` 會在清單傳回 11 個項目 (子清單)。展開後，Count 會傳回 121，因為所有子清單都已移除，所以點都在同一層清單上。
___
## 範例檔案

![List.Count](./DSCore.List.Count_img.jpg)
