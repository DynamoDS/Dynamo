## 深入資訊
`Mesh.BooleanUnion` 節點會將兩個輸入網格接合為單一網格並移除網格的重疊部分後，傳回兩個輸入網格的聯集。
在以下範例中，我們使用 `Mesh.BooleanUnion` 合併 `Mesh.Sphere` 和 `Mesh.Cuboid` 兩個網格。

(使用 `Mesh.Edges` 顯示網格的結構。)

## 範例檔案

![Example](./Autodesk.DesignScript.Geometry.Mesh.BooleanUnion_img.jpg)
