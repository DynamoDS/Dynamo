## 深入資訊
`Point.Subtract` 會將輸入點的對應座標減去輸入向量的 x、y、z 分量。這與將點平移給定向量的反向向量相同。

在以下範例中，我們使用 Code Block 指定 x、y、z 座標，然後加入一個世界 XAxis 向量作為 `vectorToSubtract`。結果是沿負 x 方向平移距離 1 的點。

___
## 範例檔案

![Subtract](./Autodesk.DesignScript.Geometry.Point.Subtract_img.jpg)

