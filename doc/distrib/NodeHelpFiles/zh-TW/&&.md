## 深入資訊
`&&` 接受兩個布林值或布林值清單，並判斷這兩個值是否皆為 True。在其他所有情況下，它都會傳回 False。

在以下範例中，我們從兩個布林值清單開始。這兩個清單搭配會有 True 和 False 值的 4 種可能組合。`&&` 只會針對第一對值傳回 True (這兩個值皆為 True)。其他值是 True 和 False 的組合，或是 False 和 False 的組合，因此 `&&` 傳回 False。
___
## 範例檔案

![&&](./&&_img.jpg)
