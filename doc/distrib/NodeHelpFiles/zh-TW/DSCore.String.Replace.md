## 深入資訊
Replace 會搜尋給定字串是否有第二個輸入字串。如果找到第二個字串，將被第三個輸入字串取代。此節點區分大小寫。在以下範例中，我們從字串「Hello World」開始。使用字串「o」作為要搜尋的字串，輸入第三個字串「Hi!」作為要變成的字串。因為字母「o」在原始字串出現兩次，所以會以字串「Hi!」取代兩次。
___
## 範例檔案

![Replace](./DSCore.String.Replace_img.jpg)

