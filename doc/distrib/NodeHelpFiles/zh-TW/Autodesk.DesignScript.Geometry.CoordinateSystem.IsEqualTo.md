## 深入資訊
IsEqualTo 會根據輸入 CoordinateSystem 的值是否彼此相等傳回布林值。在以下範例中，原點位置相同但分別為正值和負值的兩個 CoordinateSystem 經過 IsEqualTo 會傳回 false 布林值。但是，將正值的 CoordinateSystem 反轉會從 IsEqualTo 傳回 true 布林值，因為其原點的 X 值和 Y 值現在為負值。
___
## 範例檔案

![IsEqualTo](./Autodesk.DesignScript.Geometry.CoordinateSystem.IsEqualTo_img.jpg)

