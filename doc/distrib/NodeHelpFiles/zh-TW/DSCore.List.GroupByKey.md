## 深入資訊
`List.GroupByKey` 會根據鍵清單將輸入清單中的項目分組。鍵清單中的元素數應與輸入清單中的元素數相同。原始清單中對應相符鍵的項目在輸出清單中會群組在一起。

在以下範例中，我們使用一個 0 到 9 之間的數字範圍作為輸入清單，也使用 10 個介於 0 到 4 之間的隨機數字產生清單作為鍵。`List.GroupByKey` 會輸出元素的巢狀清單，根據對應的鍵進行分組，也會輸出唯一鍵的清單。
___
## 範例檔案

![List.GroupByKey](./DSCore.List.GroupByKey_img.jpg)
