## 深入資訊
`Curve.Length` 會傳回輸入曲線的長度。

在以下範例中，我們先使用 `NurbsCurve.ByControlPoints` 節點，以一組隨機產生的點作為輸入建立一條 NurbsCurve。我們可以使用 `Curve.Length` 節點找出這條曲線的長度。

___
## 範例檔案

![Length](./Autodesk.DesignScript.Geometry.Curve.Length_img.jpg)

