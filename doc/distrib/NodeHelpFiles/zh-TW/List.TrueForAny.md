## 深入資訊
`List.TrueForAny` 會傳回布林值，顯示 `queryFunction` 輸入中的條件對清單中至少一個項目而言是否為 True。`queryFunction` 輸入必須是布林值。

以下範例顯示用來演算輸入清單的兩個函數。第一個函數使用大於 (>) 節點演算是否有任何一個清單值大於 11，結果傳回 False。第二個函數使用小於 (<) 節點演算是否有任何一個清單值小於 5，結果傳回 True。
___
## 範例檔案

![List.TrueForAny](./List.TrueForAny_img.jpg)
