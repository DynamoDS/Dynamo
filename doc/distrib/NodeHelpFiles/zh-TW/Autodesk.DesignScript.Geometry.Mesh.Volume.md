## 深入資訊
`Mesh.Volume` 會傳回實體非相交網格的體積。在以下範例中，使用 `Mesh.Cone` 和 `Number.Slider` 建立圓錐，然後使用該圓錐作為輸入計算三角形數。`Mesh.Volume` 也可用於判斷網格的曲面是否封閉。事實上，如果網格有開放的曲面，節點會報告 `null` 值。

## 範例檔案

![Example](./Autodesk.DesignScript.Geometry.Mesh.Volume_img.jpg)
