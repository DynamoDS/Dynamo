## 深入資訊
`Mesh.MakeWatertight` 透過對原始網格取樣，產生一個實體、無縫且可進行 3D 列印的網格。它可讓您快速解析具有許多問題 (例如自身相交、重疊和非流形幾何圖形) 的網格。此方法會計算一個狹窄範圍的距離場，並使用移動的立方塊演算法產生新的網格，但不會投影回原始網格。這比較適合有許多瑕疵或困難問題 (例如自身相交) 的網格物件。
以下範例分別顯示一個有縫及無縫的花瓶。

## 範例檔案

![Example](./Autodesk.DesignScript.Geometry.Mesh.MakeWatertight_img.jpg)
