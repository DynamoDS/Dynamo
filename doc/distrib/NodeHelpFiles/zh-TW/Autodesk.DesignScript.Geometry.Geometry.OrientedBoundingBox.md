## 深入資訊
`Geometry.OrientedBoundingBox` 會傳回由其包含的幾何圖形定義、定向的最小體積框。BoundingBox 不會顯示，但可以使用 `BoundingBox.ToPolySurface` 節點以 PolySurface 傳回。

在以下範例中，為聯集後的實體建立 BoundingBox，並以 PolySurface 表示。
___
## 範例檔案

![Geometry.OrientedBoundingBox](./Autodesk.DesignScript.Geometry.Geometry.OrientedBoundingBox_img.jpg)
