## 深入資訊
`Line.ByStartPointEndPoint` 會在兩個輸入點之間建立一條線。

在以下範例中，我們使用兩個 Code Block 指定兩點的 X、Y、Z 座標，然後在兩點之間建立一條線。

___
## 範例檔案

![ByStartPointEndPoint](./Autodesk.DesignScript.Geometry.Line.ByStartPointEndPoint_img.jpg)

