## 深入資訊
如果輸入清單是空的，則 `List.IsEmpty` 會傳回布林值 True，如果輸入清單不是空的，則會傳回 False。

在以下範例中，我們先使用 Code Block 產生一個從 0 到 5，以 1 遞增的數字範圍。我們將此清單輸入到 `List.IsEmpty` 節點中。由於清單不是空的，因此 `List.IsEmpty` 會傳回 False 值。
___
## 範例檔案

![List.IsEmpty](./DSCore.List.IsEmpty_img.jpg)
