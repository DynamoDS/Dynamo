## 深入資訊
`List.SetIntersection` 會傳回一個包含兩個輸入清單中都有的物件的新清單。

在以下範例中，`List.SetIntersection` 會將一個 0 到 9 (以 1 遞增) 的清單與一個 0 到 18 (以 2 遞增) 的清單進行比較，並傳回 10 以下的偶數，因為這些數都在 list1 和 list2 中。
___
## 範例檔案

![List.SetIntersection](./DSCore.List.SetIntersection_img.jpg)
