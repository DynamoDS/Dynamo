## 深入資訊
`List.FirstIndexOf` 會在給定清單中搜尋是否有 Item 輸入中指定的項目，並傳回該項目的第一個例證在清單中的索引。如果在清單中找不到該項目，節點會傳回 -1。

在以下範例中，我們先使用 10 個介於 0 到 4 之間的隨機數字產生一個清單。使用 `List.FirstIndexOf` 節點，我們搜尋特定項目並擷取在清單中首次找到該項目的索引。
___
## 範例檔案

![List.FirstIndexOf](./DSCore.List.FirstIndexOf_img.jpg)
