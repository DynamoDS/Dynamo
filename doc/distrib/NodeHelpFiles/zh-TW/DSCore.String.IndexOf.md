## 深入資訊
IndexOf 會搜尋給定字串是否有第二個輸入字串。如果找到第二個字串，此節點會傳回字串第一次出現的第一個字元的索引。此節點預設區分大小寫。「ignoreCase」輸入可以使用布林值，讓節點忽略字串的大小寫。在以下範例中，我們使用字串「Hello World」並搜尋字元「o」，此字母在字串中出現兩次，但 IndexOf 節點只傳回第一次出現的索引。
___
## 範例檔案

![IndexOf](./DSCore.String.IndexOf_img.jpg)

