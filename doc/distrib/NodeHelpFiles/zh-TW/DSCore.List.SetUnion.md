## 深入資訊
`List.SetUnion` 會合併兩個輸入清單的物件並刪去重複值來傳回一個新清單。

在以下範例中，將一個 0 到 9 (以 1 遞增) 的清單與一個 0 到 18 (以 2 遞增) 的清單合併。產生的清單包含 list1 和 list2 中刪去重複項 (0、2、4 等) 後的所有元素。
___
## 範例檔案

![List.SetUnion](./DSCore.List.SetUnion_img.jpg)
