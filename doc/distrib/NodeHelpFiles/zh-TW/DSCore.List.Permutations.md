## 深入資訊
`List.Permutations` 會建立一個巢狀清單，其中包含給定輸入清單中給定長度的所有排列組合。

在以下範例中，我們使用 Code Block 建立一個從 0 到 5，以 1 遞增的數字範圍，然後使用 length 輸入為 3 的 `List.Permutations` 節點，從 [0,1,2,3,4,5] 範圍中產生 3 個元素的所有排列組合。
___
## 範例檔案

![List.Permutations](./DSCore.List.Permutations_img.jpg)
