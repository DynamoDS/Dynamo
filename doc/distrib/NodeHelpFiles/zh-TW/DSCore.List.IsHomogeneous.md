## 深入資訊
`List.IsHomogeneous` 會根據清單中的元素是否都是相同類型傳回布林值。

在以下範例中，將一個單純的數字清單與插入字串的同一個清單進行比較。原始清單是同質的 (只有數字)，因此 `List.IsHomogeneous` 會傳回 True。新清單不是同質的 (有數字和字串)，因此 `List.IsHomogeneous` 會傳回 False。
___
## 範例檔案

![List.IsHomogeneous](./DSCore.List.IsHomogeneous_img.jpg)
