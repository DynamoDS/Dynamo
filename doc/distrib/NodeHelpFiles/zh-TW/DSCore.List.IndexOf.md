## 深入資訊
`List.IndexOf` 會傳回清單中給定元素的索引位置。如果清單中找不到給定元素，或其資料類型與清單元素不符 (例如，輸入是倍精數，而清單元素是字串)，則 `List.IndexOf` 會傳回空值。

請注意，即使元素在清單中出現多次，此節點也只會傳回第一個索引。若要傳回多個索引，請改用 `List.AllIndicesOf`。
___
## 範例檔案

![List.IndexOf](./DSCore.List.IndexOf_img.jpg)
