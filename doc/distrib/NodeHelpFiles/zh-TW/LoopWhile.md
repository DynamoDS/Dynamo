## 深入資訊
`LoopWhile` 會傳回迴路函數的結果，直到滿足約束。

以下範例建立一個從 1 開始加 10，直到結果大於 100 的簡單迴路。`LoopWhile` 接受變數作為其 `initialValue` 輸入，接受函數作為 `continueWhile` 和 `loopBody` 輸入。`continueWhile` 定義迴路的約束，在此範例中是「小於」函數和引數 100。`loopBody` 定義迴路的功能，在此範例中是引數為 10 的「相加」函數。
___
## 範例檔案

![LoopWhile](./LoopWhile_img.jpg)
