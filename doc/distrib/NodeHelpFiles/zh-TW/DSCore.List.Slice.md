## 深入資訊
`List.Slice` 會根據給定的開始、結束和步長，從輸入清單傳回子清單。`start` 輸入指定輸出清單的起始索引。`end` 輸入指定其結束索引。輸出清單不包括此結束索引。`step` 輸入決定分隔清單項目的索引數，預設為 1。

在以下範例中，使用 2 的起始索引和 5 的結束索引，會產生一個包含原始清單中索引為 2、3、4 的項目的新清單。
___
## 範例檔案

![List.Slice](./DSCore.List.Slice_img.jpg)
