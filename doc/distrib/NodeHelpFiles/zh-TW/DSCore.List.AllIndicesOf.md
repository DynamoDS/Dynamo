## 深入資訊
`List.AllIndicesOf` 會搜尋給定清單是否有給定項目，並傳回找到該項目之索引的清單。如果找不到項目，`List.AllIndicesOf` 會傳回空清單。

在以下範例中，我們先產生字串清單，每個字串是 A 到 D 之間的一個字母。然後，我們使用 `List.AllIndicesOf` 搜尋清單是否有字串 B，結果在索引 1 和 4 找到 B。
___
## 範例檔案

![List.AllIndicesOf](./DSCore.List.AllIndicesOf_img.jpg)
