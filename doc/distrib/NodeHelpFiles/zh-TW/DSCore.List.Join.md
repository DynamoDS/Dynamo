## 深入資訊
`List.Join` 會將輸入清單合併為單一清單。您可以在 Join 節點上使用 + 和 - 按鈕調整輸入清單的數目。

在以下範例中，我們先使用兩個 Code Block 產生兩個數字範圍。第一個清單是奇數 [1,3,5]，第二個是偶數 [0,2,4]。使用 `List.Join` 節點，我們可以將這兩個清單接合成單一清單 [1,3,5,0,2,4]。

___
## 範例檔案

![List.Join](./DSCore.List.Join_img.jpg)
