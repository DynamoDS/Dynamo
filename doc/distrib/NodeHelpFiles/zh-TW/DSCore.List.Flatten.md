## 深入資訊
`List.Flatten` 會從多維清單 (至少有一個巢狀清單的清單) 傳回一維清單 (單一層級的清單)。

在以下範例中，透過在每個格線的指定索引處建立圓球，來比較點格線的兩個清單結構。展開的點清單會在索引位置傳回 1 點。未展開的清單會在索引位置傳回 10 點，因為每個索引都包含一排點。
___
## 範例檔案

![List.Flatten](./DSCore.List.Flatten_img.jpg)
