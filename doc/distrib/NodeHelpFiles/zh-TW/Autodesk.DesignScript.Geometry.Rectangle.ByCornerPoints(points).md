## 深入資訊
Rectangle.ByCornerPoints 節點接受四個輸入點的清單以建立矩形。輸入點必須反映矩形形狀。在此範例中，使用兩個數字滑棒在矩形頂點處透過 Points.ByCoordinates 建立點。一個類似但不同的 Rectangle.ByCornerPoints 節點使用四個不同的輸入點而非一個清單。本範例示範了兩個節點。
___
## 範例檔案

![ByCornerPoints (points)](./Autodesk.DesignScript.Geometry.Rectangle.ByCornerPoints(points)_img.jpg)

