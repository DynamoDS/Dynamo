## 深入資訊
Offset 會建立一個從輸入曲面以距離輸入指定的距離偏移的新曲面。正距離將根據曲面的法線方向建立偏移曲面，負距離將向曲面相反側偏移。在以下範例中，我們先使用 BySweep2Rails 節點建立一個曲面，然後使用數字滑棒控制 Offset 節點中的偏移距離。
___
## 範例檔案

![Offset](./Autodesk.DesignScript.Geometry.Plane.Offset_img.jpg)

