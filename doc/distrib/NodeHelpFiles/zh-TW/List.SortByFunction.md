## 深入資訊
`List.SortByFunction` 會傳回依函數排序的清單。

以下範例繪製了一條線，通過使用不同函數排序的隨機放置點的清單。通過未排序清單的第一條曲線沒有方向性。使用每個點的 X 或 Y 分量將清單排序，會產生分別穿過 X 和 Y 的方向線。
___
## 範例檔案

![List.SortByFunction](./List.SortByFunction_img.jpg)
