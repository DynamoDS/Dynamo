## 深入資訊
`==` 節點是「等於」運算子。如果 `x` 輸入等於 `y` 輸入，則會傳回 True。如果兩個值不相等，則會傳回 False。使用此節點可以比較數字和非數字，例如字串、Revit 物件、幾何圖形等。

在以下範例中，使用 `==` 節點判斷 x 輸入和 y 輸入是否相等。我們使用兩個數字滑棒控制 `==` 運算子的輸入。
___
## 範例檔案

![==](./==_img.jpg)
