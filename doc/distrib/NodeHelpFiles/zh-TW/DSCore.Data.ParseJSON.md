## 深入資訊
`Data.ParseJSON` 會從 JSON 格式轉換值。JSON 檔案格式是儲存結構化資料的慣例。事實上，Dynamo 自己的檔案格式就是像 JSON 一樣結構化的格式。

以下範例將某個字典 (JSON 格式) 還原序列化為 Dynamo 字典。
___
## 範例檔案

![Data.ParseJSON](./DSCore.Data.ParseJSON_img.jpg)
