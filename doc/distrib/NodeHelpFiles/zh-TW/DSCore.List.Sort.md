## 深入資訊
`List.Sort` 會根據資料類型的內建順序組織項目清單。例如，數字清單從最小排到最大，而字串清單則依字母順序排序。

在以下範例中，我們使用 `Math.RandomList` 先產生一個介於 0 到 1 的隨機數清單，然後乘以 10，並使用 `Math.Floor` 作業得到一個介於 0 到 9 的隨機整數清單。`List.Sort` 再建立一個從最小到最大數字排序的清單。
___
## 範例檔案

![List.Sort](./DSCore.List.Sort_img.jpg)
