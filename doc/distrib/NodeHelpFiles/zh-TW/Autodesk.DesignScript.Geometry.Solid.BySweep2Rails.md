## 深入資訊
`Solid.BySweep2Rails` 會從沿兩條曲線軌跡掃掠的曲線輪廓傳回新實體。

在以下範例中，透過沿著作為軌跡的兩條弧掃掠圓傳回新實體。在 `path` 和 `guideRail` 中切換弧輸入，可變更掃掠要與哪條弧互垂。

___
## 範例檔案

![BySweep2Rails](./Autodesk.DesignScript.Geometry.Solid.BySweep2Rails_img.jpg)

