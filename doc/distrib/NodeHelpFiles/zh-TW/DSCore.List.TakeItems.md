## 深入資訊
`List.TakeItems` 會從輸入清單的開頭傳回給定數目的項目。如果 `amount` 輸入為負數，則會以相反順序從清單的結尾取得項目。

在以下範例中，我們先產生一個從 0 到 9，以 1 遞增的數字範圍。我們對 `amount` 輸入使用整數滑棒，指定要從原始清單中取得的元素數。輸入 4，表示輸出清單包含原始清單的前 4 個元素。
___
## 範例檔案

![List.TakeItems](./DSCore.List.TakeItems_img.jpg)
