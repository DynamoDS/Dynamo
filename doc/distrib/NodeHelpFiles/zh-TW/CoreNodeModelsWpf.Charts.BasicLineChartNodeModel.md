## 深入資訊

Index-Value Line Plot 會沿著一系列的點繪製一條或多條帶有標示和不同顏色的線，建立一個折線圖。在 labels 輸入中輸入之清單中的字串值數目，決定圖中的線條數目及其對應的標示。

在 values 輸入中輸入之倍精度值清單的清單決定每個點沿著線的位置。在 colors 輸入中輸入顏色值清單，為每條線指定顏色。
___
## 範例檔案

![Index-Value Line Plot](./CoreNodeModelsWpf.Charts.BasicLineChartNodeModel_img.jpg)

