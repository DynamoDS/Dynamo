## 深入資訊
`Mesh.Translate (vector,distance)` 節點會將網格沿著給定的 `direction` 平移給定的 `distance`。

在以下範例中，將一個簡單網格沿著向量定義的方向平移，並以兩點建構的直線表示。使用 `Line.ByStartPointEndPoint` 節點顯示平移方向。

## 範例檔案

![Example](./Autodesk.DesignScript.Geometry.Mesh.Translate(mesh,%20vector,%20distance)_img.jpg)
