## 深入資訊
And 接受任意的布林輸入數。您可以使用 And 節點上的「+」和「-」按鈕增加或減少輸入數。每個輸入都必須是單一布林值。如果所有輸入都為 true，And 會傳回「true」，否則會傳回「false」。在以下範例中，我們有三個布林切換作為 And 節點的輸入。當全部三個都設定為「true」時，And 會傳回「true」。
___
## 範例檔案

![And](./CoreNodeModels.Logic.And_img.jpg)

