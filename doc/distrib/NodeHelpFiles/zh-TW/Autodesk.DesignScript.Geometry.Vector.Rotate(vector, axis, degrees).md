## 深入資訊
`Vector.Rotate (vector, axis, degrees)` 會傳回繞著輸入軸旋轉給定度數的新向量。

在以下範例中，以線表示的向量會繞 Z 軸向量旋轉。數字滑棒控制其旋轉度數。請嘗試為不同的旋轉軸插入不同的向量。
___
## 範例檔案

![Vector.Rotate](./Autodesk.DesignScript.Geometry.Vector.Rotate(vector,%20axis,%20degrees)_img.jpg)
