## 深入資訊
EndsWith 會根據給定字串是否以第二個輸入字串結束傳回布林值。此節點預設區分大小寫。「ignoreCase」輸入可以使用布林值，讓節點忽略字串的大小寫。在以下範例中，我們使用字串「Hello World」作為原始字串，並使用第二個字串「orld」作為要搜尋的字串。因為「Hello World」以字串「orld」結束，所以 EndsWith 節點傳回 true。
___
## 範例檔案

![EndsWith](./DSCore.String.EndsWith_img.jpg)

