## 深入資訊
`List.Reorder` 會傳回根據索引清單重新排序項目的新清單。

在以下範例中，透過手動變更索引順序來建立新清單。每個輸入索引都會為原始清單項目指定清單中的新位置。例如，item0 為 1，因此新清單中的第一個項目為 2.4，即原始清單中的索引 1。Item1 為 5，因此新清單中的第二個項目為 12，即原始清單中的索引 5，依此類推。
___
## 範例檔案

![List.Reorder](./DSCore.List.Reorder_img.jpg)
