## 深入資訊
CountOccurrences 會搜尋給定字串是否有第二個輸入字串，並傳回在第一個字串中找到第二個字串的次數。此節點預設區分大小寫。「ignoreCase」輸入可以使用布林值，讓節點忽略字串的大小寫。在以下範例中，我們使用字串「Hello World」並搜尋字串「L」。因為 ignoreCase 設定為 true，所以 CountOccurrences 在「Hello World」中找到給定字串三次。
___
## 範例檔案

![CountOccurrences](./DSCore.String.CountOccurrences_img.jpg)

