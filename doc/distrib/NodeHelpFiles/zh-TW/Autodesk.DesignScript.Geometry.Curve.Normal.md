## 深入資訊
`Curve.Normal` 會傳回輸入曲線的法線向量。此節點的曲線必須在同一個平面。

在以下範例中，我們先使用 `NurbsCurve.ByControlPoints` 節點，以一組隨機產生的點作為輸入建立一條 NurbsCurve。我們使用 `Curve.Normal` 節點找出這條曲線的法線。

___
## 範例檔案

![Normal](./Autodesk.DesignScript.Geometry.Curve.Normal_img.jpg)

