## 深入資訊
`>` 節點是「大於」運算子。如果 `x` 輸入大於 `y` 輸入，則傳回 True。如果 `x` 小於 `y`，或兩個值相等，則傳回 False。在以下範例中，使用 `>` 節點判斷 `x` 輸入是否大於 `y` 輸入。我們使用兩個數字滑棒控制 `>` 運算子的輸入。
___
## 範例檔案

![>](./MK3H4H3ILN766UIQEAC53XJHO5F6H7XDRRBJMXCTVKYDLUFWWIQQ_img.jpg)
