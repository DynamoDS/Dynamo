## 深入資訊
`PolyCurve.Curves` 會傳回組成輸入 PolyCurve 的個別曲線清單。

在以下範例中，我們先產生一組隨機點，接著使用 `PolyCurve.ByPoints` 建立一條開放的 PolyCurve，然後使用 `PolyCurve.Curves` 產生組成該 PolyCurve 的個別曲線清單。

___
## 範例檔案

![Curves](./Autodesk.DesignScript.Geometry.PolyCurve.Curves_img.jpg)

