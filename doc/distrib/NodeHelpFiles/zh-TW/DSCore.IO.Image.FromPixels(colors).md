## 深入資訊
FromPixels 會從輸入的二維顏色陣列建立一個影像物件。在以下範例中，我們先使用 Code Block 產生 0 到 255 的數字範圍。使用 Color.ByARGB 節點從此範圍建立顏色，將此節點的交織設定為「笛卡兒積」建立一個二維陣列。然後，我們使用 Image.FromPixels 建立影像。使用 Watch Image 節點可以預覽建立的影像。
___
## 範例檔案

![FromPixels (colors)](./DSCore.IO.Image.FromPixels(colors)_img.jpg)

