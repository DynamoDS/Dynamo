## In-Depth
`TSplineSurface.ByBoxCorners` 透過給定的 `lowPoint` 和 `highPoint`，加上一個輸入和每個方向的 X、Y、Z 跨度數，建立 T 雲形線方塊曲面。使用 `symmetry` 輸入可以指定對稱選項。`inSmoothMode` 布林輸入可在平滑預覽和方塊模式之間切換。

## 範例檔案

![Example](./Autodesk.DesignScript.Geometry.TSpline.TSplineSurface.ByBoxCorners_img.jpg)
