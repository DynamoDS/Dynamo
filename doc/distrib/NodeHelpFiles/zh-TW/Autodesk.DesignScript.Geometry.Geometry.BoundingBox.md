## 深入資訊
`Geometry.BoundingBox` 會傳回由其包含的幾何圖形定義的框。BoundingBox 不會顯示，但可以使用 `BoundingBox.ToPolySurface` 節點以 PolySurface 傳回。

在以下範例中，為聯集後的實體建立 BoundingBox，並以 PolySurface 表示。

___
## 範例檔案

![BoundingBox](./Autodesk.DesignScript.Geometry.Geometry.BoundingBox_img.jpg)

