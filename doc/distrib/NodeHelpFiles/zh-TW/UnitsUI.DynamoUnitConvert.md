## 深入資訊
`Convert By Units` 會將給定單位的輸入值轉換成不同單位系統的對應單位。可用選項取決於在第一個下拉式清單中選取的單位類型。使用箭頭按鈕可從左到右交換單位。

在此範例中，我們使用 `Convert By Units` 在華氏溫度與攝氏溫度之間轉換。首先，在第一個下拉式清單中選取「Temperature」，下方兩個下拉式清單中就只會顯示與溫度相關的選項。
___
## 範例檔案

![Convert By Units](./UnitsUI.DynamoUnitConvert_img.jpg)
