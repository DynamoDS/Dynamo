## 深入資訊
`<=` 節點是「小於或等於」運算子。如果 `x` 輸入小於 `y` 輸入，或兩個值相等，則傳回 True。如果 `x` 大於 `y`，則此運算子會傳回 False。在以下範例中，使用 `<=` 節點判斷 `x` 輸入是否小於或等於 `y` 輸入。我們使用兩個數字滑棒控制 `<=` 運算子的輸入。
___
## 範例檔案

![<=](./WYAIBXELRGBNFIV7634PG4K4DE4WCTOFKPGSEPXSDAZLRDEBLBTA_img.jpg)
