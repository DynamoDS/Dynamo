## 深入資訊
`List.DropItems` 會從清單中移除 `amount` 輸入指定的項目數。如果 `amount` 輸入為正數，將從清單開頭移除項目。如果 `amount` 輸入為負數，將從清單結尾移除項目。

在以下範例中，我們先使用 `Range` 建立一個清單，然後使用 `amount` 輸入為 1 的 `List.DropItems` 從清單中移除第一個項目。由於 `amount` 輸入為正數，因此會從清單的開頭移除項目。
___
## 範例檔案

![List.DropItems](./DSCore.List.DropItems_img.jpg)
