## 深入資訊
`PolyCurve.ByPoints` 會從一組頂點建立接合曲線。此節點也能使用 `connectLastToFirst` 輸入在封閉造型和開放造型之間切換。

以下範例先沿一個圓建立點，然後使用 `PolyCurve.ByPoints` 節點重新繪製點，建立一條封閉的多邊形造型。

___
## 範例檔案

![ByPoints](./Autodesk.DesignScript.Geometry.PolyCurve.ByPoints_img.jpg)

