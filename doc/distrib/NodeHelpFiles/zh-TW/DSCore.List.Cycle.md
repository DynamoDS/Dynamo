## 深入資訊
`List.Cycle` 接受輸入清單，並傳回將輸入清單重複 `amount` 輸入指定次數的新清單。

在以下範例中，我們先使用 Code Block 產生一個從 0 到 5，以 1 遞增的數字範圍，然後使用輸入 amount 為 3 的 `List.Cycle` 節點。結果是一個由 3 個原始清單循環組成，包含 18 個元素的清單。
___
## 範例檔案

![List.Cycle](./DSCore.List.Cycle_img.jpg)
