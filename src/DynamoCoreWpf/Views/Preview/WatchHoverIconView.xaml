<UserControl x:Class="Dynamo.Controls.WatchHoverIconView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="30"
             d:DesignWidth="30"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity">

    <Canvas Canvas.Top="0"
            Canvas.Left="0">
        <Button Background="Transparent"
                BorderBrush="Transparent"
                x:Name="MarkerBtn"
                Canvas.Top="{Binding MidPoint.Y}"
                Canvas.Left="{Binding MidPoint.X}"
                Height="{Binding MarkerSize}"
                Width="{Binding MarkerSize}"
                Command="{Binding PlaceWatchNodeCommand}">
            <StackPanel Background="Transparent">
                <Image>
                    <Image.Style>
                        <Style TargetType="{x:Type Image}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Path=IsHalftone, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Value="True">
                                    <Setter Property="Source" Value="/DynamoCoreWpf;component/UI/Images/Watch_GreyOut_16px.png"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Path=IsHalftone, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Value="False">
                                    <Setter Property="Source" Value="/DynamoCoreWpf;component/UI/Images/Watch_16px.png"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                    </Image>
            </StackPanel>
        </Button>
    </Canvas>
</UserControl>

