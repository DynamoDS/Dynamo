<UserControl x:Class="Dynamo.Controls.ConnectorContextMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="clr-namespace:Dynamo.UI"
             xmlns:props="clr-namespace:Dynamo.Wpf.Properties;assembly=DynamoCoreWpf"
             mc:Ignorable="d"
             MouseLeave="OnMouseLeave">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="{x:Static ui:SharedDictionaryManager.DynamoColorsAndBrushesDictionaryUri}" />
            </ResourceDictionary.MergedDictionaries>
            </ResourceDictionary>
    </UserControl.Resources>
    <Canvas Canvas.Top="{Binding CurrentPosition.Y}"
            Canvas.Left="{Binding CurrentPosition.X}">
        <Canvas.ContextMenu>
            <ContextMenu Visibility="Visible">
                <MenuItem Header="{x:Static props:Resources.ConnectorContextMenuHeaderBreakConnections}" 
                          Command="{Binding BreakConnectionsSurrogateCommand}"/>
                <MenuItem Header="{x:Static props:Resources.ConnectorContextMenuHeaderSelectConnected}" 
                          Command="{Binding SelectConnectedSurrogateCommand}"/>
                <MenuItem Command="{Binding HideConnectorSurrogateCommand}">
                    <MenuItem.Style>
                        <Style TargetType="MenuItem">
                            <Setter Property="IsChecked" Value="False"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsCollapsed}" Value="False">
                                    <Setter Property="Header" Value="Hide Wire"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsCollapsed}" Value="True">
                                    <Setter Property="Header" Value="Show Wire"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </MenuItem.Style>
                </MenuItem>
            </ContextMenu>
        </Canvas.ContextMenu>
    </Canvas>
</UserControl>
