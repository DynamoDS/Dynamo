<UserControl x:Class="Dynamo.Controls.ConnectorAnchorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="30"
             d:DesignWidth="30"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:ui="clr-namespace:Dynamo.UI"
             xmlns:props="clr-namespace:Dynamo.Wpf.Properties;assembly=DynamoCoreWpf"
             MouseLeave="OnMouseLeave">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="{x:Static ui:SharedDictionaryManager.DynamoColorsAndBrushesDictionaryUri}" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Canvas Canvas.Top="0"
            Canvas.Left="0">
        <!--wire anchor-->
        <Button Background="Transparent"
                BorderBrush="Transparent"
                x:Name="AnchorButton"
                Canvas.Top="{Binding CurrentPosition.Y}"
                Canvas.Left="{Binding CurrentPosition.X}"
                Height="{Binding AnchorSize}"
                Width="{Binding AnchorSize}">
            <StackPanel Background="Transparent">
                <Image>
                    <Image.Style>
                        <Style TargetType="{x:Type Image}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Path=IsHalftone, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Value="True">
                                    <Setter Property="Source" Value="/DynamoCoreWpf;component/UI/Images/wire_anchor_12_12.png"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Path=IsHalftone, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Value="False">
                                    <Setter Property="Source" Value="/DynamoCoreWpf;component/UI/Images/wire_anchor_12_12.png"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                </Image>
            </StackPanel>
            <Button.RenderTransform>
                <TranslateTransform X="-7.5" Y="-7.5" />
            </Button.RenderTransform>
        </Button>

        <!--watch icon-->
        <Button Background="Transparent"
                BorderBrush="Transparent"
                x:Name="WatchIconButton"
                Canvas.Top="{Binding CurrentPosition.Y}"
                Canvas.Left="{Binding CurrentPosition.X}"
                Height="{Binding MarkerSize}"
                Width="{Binding MarkerSize}"
                Command="{Binding PlaceWatchNodeCommand}"
                MouseEnter="OnWatchIconHover"
                MouseLeave="OnWatchIconUnhover">
            <Button.Style>
                <Style TargetType="{x:Type Button}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding CanDisplayIcons}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding CanDisplayIcons}" Value="False">
                            <Setter Property="Visibility" Value="Hidden"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
            <StackPanel Background="Transparent">
                <Image>
                    <Image.Style>
                        <Style TargetType="{x:Type Image}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Path=IsHalftone, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Value="True">
                                    <Setter Property="Source" Value="/DynamoCoreWpf;component/UI/Images/watch_hidden_16_16.png"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Path=IsHalftone, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Value="False">
                                    <Setter Property="Source" Value="/DynamoCoreWpf;component/UI/Images/Watch_16_16.png"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Path=WatchIconPreviewOn, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Value="True">
                                    <Setter Property="Source" Value="/DynamoCoreWpf;component/UI/Images/watch_default_16_16.png"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                </Image>
            </StackPanel>
            <Button.RenderTransform>
                <TranslateTransform X="-30" Y="-37" />
            </Button.RenderTransform>
        </Button>
        <!--pin icon-->
        <Button Background="Transparent"
                BorderBrush="Transparent"
                x:Name="PinIconButton"
                Canvas.Top="{Binding CurrentPosition.Y}"
                Canvas.Left="{Binding CurrentPosition.X}"
                Height="{Binding MarkerSize}"
                Width="{Binding MarkerSize}"
                Command="{Binding PinConnectorCommand}"
                MouseEnter="OnPinIconHover"
                MouseLeave="OnPinIconUnhover">
            <Button.Style>
                <Style TargetType="{x:Type Button}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Path=CanDisplayIcons, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=CanDisplayIcons, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Value="False">
                            <Setter Property="Visibility" Value="Hidden"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
            <StackPanel Background="Transparent">
                <Image>
                    <Image.Style>
                        <Style TargetType="{x:Type Image}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Path=IsHalftone, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Value="True">
                                    <Setter Property="Source" Value="/DynamoCoreWpf;component/UI/Images/pin_hidden_16_16.png"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Path=IsHalftone, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Value="False">
                                    <Setter Property="Source" Value="/DynamoCoreWpf;component/UI/Images/Pin_16_16.png"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Path=PinIconPreviewOn, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Value="True">
                                    <Setter Property="Source" Value="/DynamoCoreWpf;component/UI/Images/pin_default_16_16.png"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                </Image>
            </StackPanel>
            <Button.RenderTransform>
                <TranslateTransform X="0" Y="-37" />
            </Button.RenderTransform>
        </Button>
    </Canvas>
</UserControl>


