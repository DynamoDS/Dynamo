<UserControl x:Class="Dynamo.UI.Controls.InCanvasSearchControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:resx="clr-namespace:Dynamo.Properties;assembly=DynamoCore"
             xmlns:ui="clr-namespace:Dynamo.UI"
             xmlns:uicontrols="clr-namespace:Dynamo.UI.Controls"
             MinWidth="250"
             IsVisibleChanged="OnInCanvasSearchControlVisibilityChanged"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:SharedResourceDictionary Source="{x:Static ui:SharedDictionaryManager.SidebarGridDictionaryUri}" />
                <ui:SharedResourceDictionary Source="{x:Static ui:SharedDictionaryManager.DynamoColorsAndBrushesDictionaryUri}" />
                <ui:SharedResourceDictionary Source="{x:Static ui:SharedDictionaryManager.DynamoConvertersDictionaryUri}" />
                <ui:SharedResourceDictionary Source="{x:Static ui:SharedDictionaryManager.DynamoModernDictionaryUri}" />
            </ResourceDictionary.MergedDictionaries>

            <!--
                The result rows follow the canvas theme, while MemberGroupMemberStyle and
                MemberTemplate stay dark for the library sidebar that shares them. Those resolve
                their colours when SidebarGridStyleDictionary loads, so they cannot be overridden
                from here; the background setter can, but the hover/selected colours are template
                triggers, so the (small) template is repeated below with themed colours.
            -->
            <Style x:Key="SearchMemberStyle"
                   BasedOn="{StaticResource MemberGroupMemberStyle}"
                   TargetType="{x:Type ListBoxItem}">
                <EventSetter Event="PreviewMouseLeftButtonUp" Handler="OnMouseLeftButtonUp" />
                <EventSetter Event="MouseEnter" Handler="OnMouseEnter" />
                <EventSetter Event="MouseLeave" Handler="OnMouseLeave" />
                <Setter Property="Background" Value="{StaticResource InCanvasSearchResultBackgroundBrush}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ListBoxItem}">
                            <Border Background="{TemplateBinding Background}">
                                <ContentPresenter ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  Content="{TemplateBinding Content}" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource InCanvasSearchResultHoverBrush}" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{StaticResource InCanvasSearchResultHoverBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!--  Copy of MemberTemplate (SidebarGridStyleDictionary.xaml) with themed colours.  -->
            <DataTemplate x:Key="InCanvasSearchMemberTemplate">
                <StackPanel Name="ImageAndNodeName"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <StackPanel Visibility="{Binding Path=AutoCompletionNodeMachineLearningInfo.DisplayIcon, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock
                            HorizontalAlignment="Center"
                            MinWidth="25"
                            MaxWidth="33"
                            FontSize="11"
                            Margin="5,12,0,0"
                            Visibility="{Binding Path=AutoCompletionNodeMachineLearningInfo.IsByRecommendation, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Text="{Binding AutoCompletionNodeMachineLearningInfo.ConfidenceScore, StringFormat= {}{0} %, Mode=OneWay, Converter={StaticResource ConfidenceScoreFormattingConverter}}" Foreground="{StaticResource DynamoStandardLabelTextBrush}">
                        </TextBlock>
                        <Image HorizontalAlignment="Center"
                               Height="14"
                               VerticalAlignment="Center"
                               Width="14"
                               Source="pack://application:,,,/DynamoCoreWpf;component/UI/Images/clock-icon.png"
                               Margin="7,10,8,0"
                               Visibility="{Binding Path=AutoCompletionNodeMachineLearningInfo.IsByUse, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    </StackPanel>
                    <Image HorizontalAlignment="Left"
                           Height="36"
                           Margin="2,0,16,0"
                           VerticalAlignment="Center"
                           Width="24"
                           Source="{Binding SmallIcon}" />
                    <Grid Margin="-10,0,0,0">
                        <Rectangle Fill="{StaticResource InCanvasSearchResultHighlightBrush}"
                                   HorizontalAlignment="Stretch"
                                   VerticalAlignment="Stretch">
                            <Rectangle.Margin>
                                <MultiBinding Converter="{StaticResource SearchHighlightMarginConverter}">
                                    <Binding ElementName="memberName" />
                                    <Binding Path="DataContext"
                                             RelativeSource="{RelativeSource Mode=FindAncestor, AncestorType={x:Type UserControl}}" />
                                </MultiBinding>
                            </Rectangle.Margin>
                        </Rectangle>
                        <TextBlock Grid.Column="1"
                                   Name="memberName"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Center"
                                   FontSize="13">
                            <Run Text="{Binding Name, Mode=OneWay}"
                                 Foreground="{StaticResource InCanvasSearchResultForegroundBrush}" />
                            <Run Text="{Binding Parameters, Mode=OneWay}"
                                 Foreground="{StaticResource InCanvasSearchResultSecondaryForegroundBrush}" />
                        </TextBlock>
                    </Grid>
                </StackPanel>
            </DataTemplate>

            <Style BasedOn="{StaticResource LibraryScrollViewerStyle}" TargetType="{x:Type ScrollViewer}">
                <Setter Property="Template" Value="{StaticResource LibraryScrollViewerControlTemplate}" />
            </Style>

        </ResourceDictionary>
    </UserControl.Resources>

    <Grid PreviewKeyDown="OnInCanvasSearchKeyDown">
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Name="SearchText" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Grid.ColumnSpan="2"
                    Margin="0,0,0,2"
                    Padding="5"
                    Background="{StaticResource InCanvasSearchBackgroundBrush}"
                    BorderBrush="{StaticResource InCanvasSearchBorderBrush}"
                    BorderThickness="0,0,0,1">
                <Grid>
                    <DockPanel Name="SearchIconAndTextBlockGrid" Grid.ColumnSpan="2">
                        <Image x:Name="SearchIcon"
                               Width="20px"
                               Height="20px"
                               HorizontalAlignment="Left"
                               VerticalAlignment="Center"
                               DockPanel.Dock="Right">
                            <Image.Style>
                                <Style TargetType="{x:Type Image}">
                                    <Setter Property="Source" Value="/DynamoCoreWpf;component/UI/Images/searchmagnifier.png" />
                                </Style>
                            </Image.Style>
                        </Image>
                        <TextBlock Name="SearchTextBlock"
                                   Height="18"
                                   Margin="5,1,0,0"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Center"
                                   DockPanel.Dock="Left"
                                   FontFamily="{StaticResource ArtifaktElementRegular}"
                                   FontSize="14"
                                   Foreground="{StaticResource InCanvasSearchPlaceholderBrush}"
                                   IsHitTestVisible="False"
                                   Text="{x:Static resx:Resources.SearchTextBlockText}"
                                   Visibility="{Binding Path=SearchText, Converter={StaticResource NonEmptyStringToCollapsedConverter}}" />
                    </DockPanel>
                    <TextBox Name="SearchTextBox"
                             MinWidth="250"
                             Margin="3,3,0,0"
                             VerticalAlignment="Center"
                             Background="Transparent"
                             BorderThickness="0"
                             CaretBrush="{StaticResource InCanvasSearchCaretBrush}"
                             FontFamily="{StaticResource ArtifaktElementRegular}"
                             FontSize="14"
                             Foreground="{StaticResource InCanvasSearchForegroundBrush}"
                             IsEnabled="True"
                             Text="{Binding Path=SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </Grid>
            </Border>
            
        </Grid>
        <Rectangle Grid.Row="0" Height="0px" Fill="{StaticResource InCanvasSearchAccentBrush}" VerticalAlignment="Bottom"/>
        <Border Name="FoundMembers"
                Grid.Row="1"
                MaxHeight="250"
                Background="{StaticResource InCanvasSearchResultBackgroundBrush}"
                Opacity=".9">
            <ListBox Name="MembersListBox"
                     Background="Transparent"
                     BorderBrush="Transparent"
                     ItemContainerStyle="{StaticResource SearchMemberStyle}"
                     ItemsSource="{Binding FilteredResults, NotifyOnTargetUpdated=True}"
                     PreviewMouseWheel="OnMembersListBoxMouseWheel"
                     TargetUpdated="OnMembersListBoxUpdated">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <ScrollViewer MinWidth="250"
                                      CanContentScroll="True"
                                      Style="{StaticResource InCanvasScrollViewerStyle}"
                                      Template="{StaticResource InCanvasScrollViewerControlTemplate}">
                            <ContentPresenter ContentTemplate="{StaticResource InCanvasSearchMemberTemplate}" />
                        </ScrollViewer>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Border.Style>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Path=SearchResults.Count}" Value="0">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=CurrentMode, Converter={StaticResource LibraryViewModeToBoolConverter}}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>


        <Border Margin="-3,0,-3,0">
            <Popup x:Name="toolTipPopup"
                   AllowsTransparency="True"
                   Placement="Right"
                   StaysOpen="True">
                <uicontrols:ToolTipWindow />
            </Popup>
        </Border>

    </Grid>
</UserControl>
