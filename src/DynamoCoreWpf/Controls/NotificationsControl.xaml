<UserControl
    x:Class="Dynamo.Wpf.Controls.NotificationsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:Dynamo.Controls"
    xmlns:core="clr-namespace:Dynamo.Wpf.ViewModels.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:p="clr-namespace:Dynamo.Wpf.Properties;assembly=DynamoCoreWpf"
    xmlns:viewmodels="clr-namespace:Dynamo.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:DynamoViewModel}"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">

    <UserControl.Resources>
        <core:NotificationLevelToColorConverter x:Key="NotificationLevelToColorConverter" />
        <converters:LinterIssueCountToVisibilityConverter x:Key="LinterIssueCountToVisibilityConverter" />
    </UserControl.Resources>

    <Grid HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock
            Margin="10"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Center"
            FontSize="12"
            FontStyle="Italic"
            Foreground="{Binding Path=HomeSpaceViewModel.CurrentNotificationLevel, Converter={StaticResource NotificationLevelToColorConverter}}"
            Text="{Binding Path=HomeSpaceViewModel.CurrentNotificationMessage}"
            TextAlignment="Right"
            TextTrimming="WordEllipsis">
            <TextBlock.Triggers>
                <EventTrigger RoutedEvent="Binding.TargetUpdated">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(TextBlock.Opacity)">
                                <LinearDoubleKeyFrame KeyTime="0:0:0" Value="0" />
                                <LinearDoubleKeyFrame KeyTime="0:0:0.35" Value="1" />
                                <LinearDoubleKeyFrame KeyTime="0:0:5" Value="1" />
                                <LinearDoubleKeyFrame KeyTime="0:0:5.35" Value="0" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </TextBlock.Triggers>
        </TextBlock>
        <StackPanel
            Grid.Column="1"
            Margin="30,0"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            MouseDown="StackPanel_MouseDown"
            Orientation="Horizontal"
            Visibility="{Binding LinterIssuesCount, Converter={StaticResource LinterIssueCountToVisibilityConverter}}">
            <StackPanel.Style>
                <Style TargetType="{x:Type StackPanel}">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Cursor" Value="Hand" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <Image Width="20" Source="/DynamoCoreWpf;component/UI/Images/alert.png" />
            <TextBlock Margin="10,0" Foreground="White">
                <Run Text="{Binding LinterIssuesCount, Mode=OneWay}" />
                <Run Text="{x:Static p:Resources.PersistentVisualStatusOfLinterIssues}" />
            </TextBlock>
        </StackPanel>

    </Grid>
</UserControl>
