<UserControl x:Class="Dynamo.PackageDependency.PackageDependencyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300" MinWidth="250" MinHeight="420">
    <Grid Width="400" Height="800" Background= "#222222">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid 
            Name="PackageDependencyTable" 
            HorizontalAlignment="Left" 
            Height="430"
            Width=" 300"
            Margin="10,10,0,0" 
            VerticalAlignment="Top" 
            ItemsSource="{Binding Path=Packages}"
            />
    </Grid>
</UserControl>
