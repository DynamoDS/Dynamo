    <Window x:Class="Dynamo.PackageManager.UI.InstalledPackagesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Installed Packages" Height="450" Width="560" Background="#555" MinWidth="560" MaxWidth="560" >

        <Window.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <ResourceDictionary Source="/DynamoElements;component/Themes/DynamoModern.xaml" />
                    <ResourceDictionary Source="/DynamoElements;component/Themes/DynamoConverters.xaml"/>
                    <ResourceDictionary Source="/DynamoElements;component/Themes/DynamoColorsAndBrushes.xaml" />
                </ResourceDictionary.MergedDictionaries>
            </ResourceDictionary>
        </Window.Resources>
        
        <Grid Background="Black" Margin="5">

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ScrollViewer HorizontalAlignment="Stretch" Margin="0" Name="ScrollView" VerticalAlignment="Stretch" Focusable="False"
                      VerticalScrollBarVisibility="Visible" Background="Black" BorderThickness="0" Grid.Column="0" Grid.ColumnSpan="1" Grid.Row="0" >

            <ListBox Name="SearchResultsListBox" ItemsSource="{Binding Path=LocalPackages}" BorderThickness="0" Padding="0" Background="Black" >

                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Style.Resources>
                                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#000"/>
                                <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#000" />
                            </Style.Resources>
                        </Style>

                    </ListBox.ItemContainerStyle>

                    <ListBox.ItemTemplate >

                        <DataTemplate>

                            <Border BorderBrush="#666" BorderThickness="0,1,0,0" Width="512">

                                <Border.Background>
                                    <LinearGradientBrush  StartPoint="0.5,0" EndPoint="0.5,1">
                                        <GradientStop Color="#353535" Offset="0.0" />
                                        <GradientStop Color="#353535" Offset="1.0" />
                                    </LinearGradientBrush>
                                </Border.Background>
                                
                                <StackPanel Name="SearchEle">

                                    <StackPanel Orientation="Horizontal">
                                        
                                        <StackPanel Orientation="Horizontal" Margin="10,10,30,5" Width="360">
                                            <TextBlock Text="{Binding Path=Name}" FontSize="13" Margin="0,0,10,0" Foreground="LightCyan"></TextBlock>
                                            <TextBlock Text="{Binding Path=VersionName}" FontSize="13" Foreground="LightCyan"></TextBlock>
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal" Margin="10">
                                            <TextBlock Text="Votes: " Foreground="Lavender"></TextBlock>
                                            <TextBlock Text="0" Foreground="Lavender"></TextBlock>
                                        </StackPanel>
                                        
                                    </StackPanel>
                                    
                                    <StackPanel Orientation="Vertical" Margin="10,2,0,2">
                                        
                                        <TextBlock Text="{Binding Path=RootDirectory}" FontSize="10" Foreground="Gray"></TextBlock>
                                        
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        
                                        <Button Name="TypesButton" Command="{Binding Path=ToggleTypesVisibleInManagerCommand}" Margin="5" ToolTip="See the types loaded by this package" Content="Nodes" Style="{DynamicResource ResourceKey=SBadgeButton}" />
                                        <Button Name="UninstallButton" Command="{Binding Path=UninstallCommand}" Margin="5" ToolTip="Uninstall this package, only allowed if none of the types are currently in use" Content="Uninstall" Style="{DynamicResource ResourceKey=SBadgeButton}" />
                                        <Button Name="GetLatestVersionButton"  Command="{Binding Path=GetLatestVersionCommand}" Margin="5" ToolTip="Check if there is a newer version of this package and download it." Content="Get Latest Version" Style="{DynamicResource ResourceKey=SBadgeButton}" />
                                        <Button Name="MakeNewVersionButton" Command="{Binding Path=MakeNewVersionCommand}" Margin="5" ToolTip="Published a new version of this package" Content="Publish New Version" Style="{DynamicResource ResourceKey=SBadgeButton}" />
                                        <Button Name="UpVoteButton" Command="{Binding Path=UpVoteCommand}" Margin="5" ToolTip="Vote for this package" Content="Up Vote" Style="{DynamicResource ResourceKey=SBadgeButton}" />
                                        <Button Name="DownVoteButton" Command="{Binding Path=DownVoteCommand}" Margin="5" ToolTip="Down vote this package" Content="Down Vote" Style="{DynamicResource ResourceKey=SBadgeButton}" />
                                        
                                    </StackPanel>

                                    <StackPanel Margin="10,5,5,10" Visibility="{Binding Path=TypesVisibleInManager, Converter={StaticResource BooleanToVisibilityCollapsedConverter}}">

                                        <Label Content="Loaded Custom Nodes" FontWeight="Bold" Foreground="Gray"></Label>
                                        <ListBox Name="LoadedCustomNodes" ItemsSource="{Binding Path=LoadedCustomNodes}" BorderThickness="0" Padding="0" Margin="10,0,0,0" Background="Transparent" >

                                            <ListBox.ItemContainerStyle>
                                                <Style TargetType="ListBoxItem">
                                                    <Style.Resources>
                                                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#000"/>
                                                        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#000" />
                                                    </Style.Resources>
                                                </Style>
                                            </ListBox.ItemContainerStyle>

                                            <ListBox.ItemTemplate >
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Path=Name}" Foreground="Gray" />
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>

                                        </ListBox>

                                        <!--<Label Content="Compiled nodes" Foreground="Lavender"></Label>
                                        <ListBox Name="CompiledNodes" ItemsSource="{Binding Path=LoadedTypes}" BorderThickness="0" Padding="0" Margin="10,0,0,0" Background="Black" >

                                            <ListBox.ItemContainerStyle>
                                                <Style TargetType="ListBoxItem">
                                                    <Style.Resources>
                                                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#000"/>
                                                        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#000" />
                                                    </Style.Resources>
                                                </Style>
                                            </ListBox.ItemContainerStyle>

                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Path=Name}" Foreground="Lavender" />
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>

                                        </ListBox>-->
                                    </StackPanel>
                                </StackPanel>

                            </Border>

                        </DataTemplate>

                    </ListBox.ItemTemplate>

                </ListBox>


            </ScrollViewer>
        </Grid>

    </Window>