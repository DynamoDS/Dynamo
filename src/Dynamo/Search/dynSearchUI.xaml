<UserControl x:Class="Dynamo.Nodes.dynSearchUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:interop="clr-namespace:System.Windows.Interop;assembly=PresentationCore"
             mc:Ignorable="d" 
             Visibility="Collapsed" d:DesignHeight="342" d:DesignWidth="447" x:Name="SearchControl">
    
  
  <Grid Background="Black" >

    <StackPanel x:Name="RSearchContainerStackPanel"  Margin="0">
      
      <StackPanel Name="RSearchBoxStackPanel" Orientation="Horizontal">
        
        <TextBox Name="RSearchBox" Foreground="WhiteSmoke" Background="#222" BorderThickness="0" FontSize ="18" Padding="10,15,5,10" Margin ="0" Text="" 
                       IsEnabled="True" TextChanged="SearchBox_OnTextChanged" VerticalAlignment="Stretch" Width="355"/>

        <ToggleButton Name="RSearchBoxIncludeOnline" Margin="0" Width="90" FontSize="24" Foreground="White" ToolTip="Include nodes from online" 
                      Style="{DynamicResource ResourceKey=SSearchOnlineToggleButton}" Tag="..\Images\cloud_download_arrow_white.png">
          <![CDATA[&]]>
         </ToggleButton>

      </StackPanel>

      <ScrollViewer HorizontalAlignment="Stretch" Margin="0" Name="RSearchResultsScrollView" VerticalAlignment="Stretch" 
                      VerticalScrollBarVisibility="Visible" Height="255" Background="Black" BorderThickness="0" Width="{Binding Path=ActualWidth,ElementName=SearchControl}">

          <ListBox Name="RSearchResultsListBox" ItemsSource="{Binding Path=VisibleNodes, ElementName=SearchControl}" 
                   BorderThickness="0" Padding="0" Background="Black">
            
            <ListBox.ItemTemplate>
              <DataTemplate>
                
                <Grid VerticalAlignment="Stretch"
                      Width="{Binding ElementName=RSearchResultsListBox,Path=ActualWidth}" 
                      HorizontalAlignment="Left" Background="Black">

                  <Line X1="0" X2="{Binding ElementName=RSearchResultsListBox,Path=ActualWidth}" 
                            Stroke="#111" Y1="0" Y2="0" VerticalAlignment="Top"/>
                  <TextBlock FontSize="24" Text="{Binding Path=NickName}" Foreground="AliceBlue" Padding="10,5,0,0"/>
                  <TextBlock FontSize="12" Text="{Binding Path=Description}" Foreground="Gray"  Padding="10, 39, 0, 10"/>
                  <Line X1="0" X2="{Binding ElementName=RSearchResultsListBox,Path=ActualWidth}" 
                            Stroke="DimGray" Y1="0" Y2="0" VerticalAlignment="Bottom"/>
                  
                </Grid>
                
              </DataTemplate>
            </ListBox.ItemTemplate>
            
          </ListBox>          
        </ScrollViewer>
      </StackPanel>
  </Grid>
  
  
  <UserControl.Resources>

    <Style x:Key="SSearchOnlineToggleButton" TargetType="{x:Type ToggleButton}">
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate>
            
            <Grid x:Name="container" Background="Transparent">
              <Grid x:Name="inner" Background="#333">

                <Image Width="32" Height="32" x:Name="display" Source="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Tag}"></Image>
                
                <!--<TextBlock x:Name="display" Text="{Binding Content, RelativeSource={ RelativeSource TemplatedParent}}" >
                  
                  <TextBlock.Foreground>Gray</TextBlock.Foreground>
                  <TextBlock.FontSize>30</TextBlock.FontSize>
                  <TextBlock.HorizontalAlignment>Center</TextBlock.HorizontalAlignment>
                  <TextBlock.VerticalAlignment>Center</TextBlock.VerticalAlignment>
                  
                </TextBlock>-->
                
              </Grid>
            </Grid>
            
            <ControlTemplate.Triggers>
              
              <Trigger Property="ToggleButton.IsChecked" Value="True">
                <Setter TargetName="inner" Property="Background" Value="LightBlue"/>
                <!--<Setter TargetName="display" Property="Foreground" Value="White"/>-->
              </Trigger>
              
              <Trigger Property="ToggleButton.IsMouseOver" Value="true">
                <Setter TargetName="inner" Property = "Background" Value="LightBlue"/>
                <!--<Setter TargetName="display" Property="Foreground" Value="White"/>-->
              </Trigger>
              
            </ControlTemplate.Triggers>
            
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    
    <!--Scrollbar Thumbs-->
    <Style x:Key="SScrollThumbs" TargetType="{x:Type Thumb}">
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type Thumb}">
            <Grid x:Name="Grid">
              <Rectangle HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="Auto" Height="Auto" Fill="Transparent" />
              <Border x:Name="Rectangle1" CornerRadius="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="Auto" Height="Auto"  Background="{TemplateBinding Background}" />
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="Tag" Value="Horizontal">
                <Setter TargetName="Rectangle1" Property="Width" Value="Auto" />
                <Setter TargetName="Rectangle1" Property="Height" Value="7" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!--ScrollBars-->
    <Style x:Key="{x:Type ScrollBar}" TargetType="{x:Type ScrollBar}">
      <Setter Property="Stylus.IsFlicksEnabled" Value="false" />
      <Setter Property="Foreground" Value="#333" />
      <Setter Property="Background" Value="#111" />
      <Setter Property="Width" Value="8" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type ScrollBar}">
            <Grid x:Name="GridRoot" Width="19" Background="{TemplateBinding Background}">
              <Grid.RowDefinitions>
                <RowDefinition Height="0.00001*" />
              </Grid.RowDefinitions>

              <Track x:Name="PART_Track" Grid.Row="0" IsDirectionReversed="true" Focusable="false">
                <Track.Thumb>
                  <Thumb x:Name="Thumb" Background="{TemplateBinding Foreground}" Style="{DynamicResource SScrollThumbs}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                  <RepeatButton x:Name="PageUp" Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="false" />
                </Track.IncreaseRepeatButton>
                <Track.DecreaseRepeatButton>
                  <RepeatButton x:Name="PageDown" Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="false" />
                </Track.DecreaseRepeatButton>
              </Track>
            </Grid>

            <ControlTemplate.Triggers>
              <Trigger SourceName="Thumb" Property="IsMouseOver" Value="true">
                <Setter Value="{DynamicResource ButtonSelectBrush}" TargetName="Thumb" Property="Background" />
              </Trigger>
              <Trigger SourceName="Thumb" Property="IsDragging" Value="true">
                <Setter Value="{DynamicResource DarkBrush}" TargetName="Thumb" Property="Background" />
              </Trigger>

              <Trigger Property="IsEnabled" Value="false">
                <Setter TargetName="Thumb" Property="Visibility" Value="Collapsed" />
              </Trigger>
              <Trigger Property="Orientation" Value="Horizontal">
                <Setter TargetName="GridRoot" Property="LayoutTransform">
                  <Setter.Value>
                    <RotateTransform Angle="-90" />
                  </Setter.Value>
                </Setter>
                <Setter TargetName="PART_Track" Property="LayoutTransform">
                  <Setter.Value>
                    <RotateTransform Angle="-90" />
                  </Setter.Value>
                </Setter>
                <Setter Property="Width" Value="Auto" />
                <Setter Property="Height" Value="12" />
                <Setter TargetName="Thumb" Property="Tag" Value="Horizontal" />
                <Setter TargetName="PageDown" Property="Command" Value="ScrollBar.PageLeftCommand" />
                <Setter TargetName="PageUp" Property="Command" Value="ScrollBar.PageRightCommand" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

  </UserControl.Resources>
  
</UserControl>
  
  