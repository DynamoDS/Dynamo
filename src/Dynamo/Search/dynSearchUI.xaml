<UserControl x:Class="Dynamo.Nodes.dynSearchUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Visibility="Collapsed" d:DesignHeight="278" d:DesignWidth="447" x:Name="SearchControl">
    
    <Grid Background="Black">

        <StackPanel x:Name="dynamicSearchPanel" x:FieldModifier="public"  Margin="12,10,8,12" Height="233">

            <TextBox Name="dynamicSearchBox" FontSize ="24" Padding="5" Margin ="10" Text="" IsEnabled="True" TextChanged="SearchBox_OnTextChanged" VerticalAlignment="Stretch" />

            <ScrollViewer HorizontalAlignment="Stretch" Margin="10,20,10,10" Name="resultScrollView" VerticalAlignment="Stretch" 
                          VerticalScrollBarVisibility="Visible" Height="163" Width="405">

                <ListBox Name="dynamicSearchPanelResults" ItemsSource="{Binding VisibleNodes, ElementName=SearchControl}"/> 
                
            </ScrollViewer>
        
        </StackPanel>
    </Grid>

</UserControl>
