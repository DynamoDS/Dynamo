<Window x:Class="Dynamo.Nodes.FunctionNamePrompt"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Node" Height="240" Width="238" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    
    <Grid Height="Auto" Background="Black">
        
      <Label Content="Name:" Foreground="WhiteSmoke" Height="28" HorizontalAlignment="Stretch" Margin="11,1,12,0" Name="label1" VerticalAlignment="Top" />

        <Border Margin="10,30,10,140" >

            <Border.Style>

                <Style TargetType="Border">

                    <Setter Property="Background" Value="#333" />

                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsFocused, ElementName=nameBox}" Value="true">
                            <Setter Property="Background" Value="#555" />
                            <Setter Property="Effect" >
                                <Setter.Value>
                                    <DropShadowEffect Color="Cyan" ShadowDepth="0" BlurRadius ="7" />
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>

                    </Style.Triggers>
                </Style>

            </Border.Style>

            <TextBox x:FieldModifier="public"
                 HorizontalAlignment="Stretch" 
                 Name="nameBox" 
                 Padding="10"
                 Background="Transparent"
                 Foreground="AliceBlue"
                 FontSize="14"
                 CaretBrush="White"
                 VerticalAlignment="Stretch" 
                 BorderThickness="0"
                 HorizontalScrollBarVisibility="Disabled"
                 VerticalScrollBarVisibility="Disabled"
                 TextWrapping="NoWrap" />

        </Border>
        
        <Button Content="Cancel" Style="{DynamicResource ResourceKey=STextButton}" HorizontalAlignment="Right" Margin="0,143,13,0" Name="cancelButton" VerticalAlignment="Top" Width="60" Click="Cancel_Click" IsCancel="True" TabIndex="3" />
        <Button Content="OK" Style="{DynamicResource ResourceKey=STextButton}"  HorizontalAlignment="Right" Margin="0,143,84,0" Name="okButton" VerticalAlignment="Top" Width="75" Click="OK_Click" IsDefault="True" TabIndex="2" />

        <ComboBox FontSize="14" Style="{DynamicResource ResourceKey=SComboBox}" Foreground="White" HorizontalAlignment="Stretch" Margin="10,107,10,0" Name="categoryBox" VerticalAlignment="Top" Width="192" IsEditable="True" TabIndex="1" />

        

        <Label Content="Category:" Foreground="WhiteSmoke" Height="28" HorizontalAlignment="Stretch" Margin="12,80,13,0" Name="label2" VerticalAlignment="Top" />
        
   </Grid>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes\DynamoModern.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
</Window>
