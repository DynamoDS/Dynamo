<UserControl x:Class="Dynamo.Nodes.Views.dynConnectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:vm="clr-namespace:Dynamo.Connectors"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>

        <!--<Ellipse Name="endDot" Width="6" Height="6" Fill="Black" IsHitTestVisible="False" 
                 Canvas.Top="{Binding BezStartPoint.Y}" Canvas.Left="{Binding BezStartPoint.X}"--> 

        <Path Stroke="{Binding StrokeBrush}" StrokeThickness="{Binding StrokeThickness}"
              Name="connector" MouseEnter="Highlight" MouseLeave="Unhighlight"
              Visibility="Visible">
            <Path.Data>
                <PathGeometry>
                    <PathGeometry.Figures>
                        <PathFigureCollection>
                            <PathFigure StartPoint="{Binding BezStartPoint}">
                                <PathFigure.Segments>
                                    <PathSegmentCollection>
                                        <BezierSegment Point1="{Binding BezPoint1}" Point2="{Binding BezPoint2}" Point3="{Binding BezPoint3}" />
                                    </PathSegmentCollection>
                                </PathFigure.Segments>
                            </PathFigure>
                        </PathFigureCollection>
                    </PathGeometry.Figures>
                </PathGeometry>
            </Path.Data>
        </Path>
        
        <Path Stroke="{Binding Path=StrokeBrush}" StrokeThickness="{Binding StrokeThickness}" 
              Name="plineConnector" MouseEnter="Highlight" MouseLeave="Unhighlight"
              Visibility="{Binding PlineVisibility}">
            <Path.Data>
                <PathGeometry>
                    <PathGeometry.Figures>
                        <PathFigureCollection>
                            <PathFigure StartPoint="{Binding Path=BezStartPoint}">
                                <PathFigure.Segments>
                                    <PathSegmentCollection>
                                        <LineSegment Point="{Binding BezPoint1}"/> 
                                        <LineSegment Point="{Binding BezPoint2}"/> 
                                        <LineSegment Point="{Binding BezPoint3}"/>
                                    </PathSegmentCollection>
                                </PathFigure.Segments>
                            </PathFigure>
                        </PathFigureCollection>
                    </PathGeometry.Figures>
                </PathGeometry>
            </Path.Data>
        </Path>
        
        
    </Grid>
</UserControl>
