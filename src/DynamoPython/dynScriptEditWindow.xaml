<Window x:Class="Dynamo.Nodes.dynScriptEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:avalonedit="clr-namespace:ICSharpCode.AvalonEdit;assembly=ICSharpCode.AvalonEdit"
        Title="Edit Python Script..." Height="800" Width="600">
  <Grid>
    <!--<TextBox x:FieldModifier="public"
                 HorizontalAlignment="Stretch" 
                 Margin="10,10,10,42" 
                 Name="editText" 
                 VerticalAlignment="Stretch" 
                 HorizontalScrollBarVisibility="Auto"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap"
                 AcceptsReturn="True"
                 AcceptsTab="True"/>-->

    <Border Margin="10,10,10,42" BorderThickness="1" BorderBrush="DarkGray" >
      <avalonedit:TextEditor Name="editText" FontFamily="Consolas" FontSize="10pt" ShowLineNumbers="True"/>
    </Border>

    <Button Content="Cancel" Height="23" HorizontalAlignment="Right" Margin="10,10,10,10" Name="button1" VerticalAlignment="Bottom" Width="75" Click="button1_Click" />
    <Button Content="OK" Height="23" HorizontalAlignment="Right" Margin="10,10,100,10" Name="button2" VerticalAlignment="Bottom" Width="75" Click="button2_Click" />
    
  </Grid>
</Window>
