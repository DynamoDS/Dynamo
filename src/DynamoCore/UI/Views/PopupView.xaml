<UserControl x:Class="Dynamo.UI.Views.PopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             Name="PopupWindowUserControl">
    <Grid x:Name="mainGrid" MaxWidth="{Binding Path=MaxWidth}" Background="Transparent" Opacity="{Binding Path=Opacity}" Margin="{Binding Path=Margin}">
        <Polygon Name="backgroundPolygon" Fill="{Binding Path=FrameFill}" Stroke="{Binding Path=FrameStrokeColor}" StrokeThickness="{Binding Path=FrameStrokeThickness}">
        </Polygon>
        <StackPanel Name="contentContainer" Orientation="Vertical" Background="Transparent">
            <TextBox IsReadOnly="True" Background="Transparent" BorderThickness="0"
                     Margin="{Binding Path=ContentMargin}" Text="{Binding Path=ItemDescription}"
                     Foreground="{Binding Path=TextForeground}" FontSize="{Binding Path=TextFontSize}" FontWeight="{Binding Path=TextFontWeight}"
                     TextChanged="OnTextChange"/>
        </StackPanel>
    </Grid>
</UserControl>
