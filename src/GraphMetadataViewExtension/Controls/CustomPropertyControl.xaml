<UserControl x:Class="Dynamo.GraphMetadata.Controls.CustomPropertyControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Dynamo.GraphMetadata.Controls"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="200">
    <Grid 
        DataContext="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:CustomPropertyControl}}}"
        HorizontalAlignment="Stretch">
        <StackPanel Orientation="Vertical">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="propertyNameText" 
                           Grid.Column="0"
                           IsEnabled="False"
                           Text="{Binding PropertyName, Mode=TwoWay}" />
                <Button Grid.Column="1" 
                        Background="Transparent" 
                        BorderThickness="0"
                        Command="{Binding EditPropertyNameCmd}">
                    <Button.Content>
                        <fa:ImageAwesome Icon="Pencil"
                                         Height="15"
                                         Grid.Column="1" />
                    </Button.Content>
                </Button>
                <Button Grid.Column="2"
                        Background="Transparent"
                        BorderThickness="0">
                    <Button.Content>
                        <fa:ImageAwesome Icon="Remove"
                                         Height="15"
                                         Grid.Column="1" />
                    </Button.Content>
                </Button>
            </Grid>
            <TextBox BorderBrush="Gray"
                     Background="Black"
                     Foreground="White"
                     Margin="0,5" />
        </StackPanel>
    </Grid>
</UserControl>
