<UserControl
    x:Class="Dynamo.GraphMetadata.Controls.ImageSelector"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:convertes="clr-namespace:Dynamo.GraphMetadata.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Dynamo.GraphMetadata.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:properties="clr-namespace:Dynamo.GraphMetadata.Properties"
    xmlns:ui="clr-namespace:Dynamo.UI;assembly=DynamoCoreWpf"
    d:DesignHeight="198"
    d:DesignWidth="314"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:SharedResourceDictionary Source="{x:Static ui:SharedDictionaryManager.DynamoModernDictionaryUri}" />
                <ui:SharedResourceDictionary Source="{x:Static ui:SharedDictionaryManager.DynamoConvertersDictionaryUri}" />
                <ui:SharedResourceDictionary Source="{x:Static ui:SharedDictionaryManager.DynamoColorsAndBrushesDictionaryUri}" />
            </ResourceDictionary.MergedDictionaries>
            <convertes:BooleanInverseConverter x:Key="BooleanInverseConverter" />
            <convertes:BooleansToVisibilityConverter x:Key="BooleansToVisibilityConverter" />

            <SolidColorBrush
                x:Key="ButtonOverlayBrush"
                Opacity="0.5"
                Color="Black" />
            <SolidColorBrush
                x:Key="ButtonOverlayPressedBrush"
                Opacity="0.3"
                Color="Black" />
            <SolidColorBrush
                x:Key="ButtonOverlayTextColor"
                Opacity="0.3"
                Color="White" />

            <Style x:Key="OverlayButton" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource ButtonOverlayBrush}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border
                                Background="{TemplateBinding Background}"
                                BorderBrush="Black"
                                BorderThickness="1">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:ImageSelector}}}" Value="False">
                        <Setter Property="Visibility" Value="Collapsed" />
                    </DataTrigger>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource ButtonOverlayBrush}" />
                        <Setter Property="Foreground" Value="{StaticResource ButtonOverlayTextColor}" />
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" Value="{StaticResource ButtonOverlayPressedBrush}" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border
        BorderBrush="#FF3F4040"
        BorderThickness="1"
        DataContext="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:ImageSelector}}}">
        <Grid Background="{StaticResource SearchBoxBackgroundColor}">
            <Image Source="{Binding Image}" Stretch="Uniform" />
            <TextBlock
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Text="{x:Static properties:Resources.ImageSelector_Message_NoImageSelected}">
                <TextBlock.Visibility>
                    <MultiBinding Converter="{StaticResource BooleansToVisibilityConverter}">
                        <Binding Converter="{StaticResource BooleanInverseConverter}" Path="IsMouseOver" />
                        <Binding Converter="{StaticResource BooleanInverseConverter}" Path="HasImage" />
                    </MultiBinding>
                </TextBlock.Visibility>
            </TextBlock>

            <Button
                x:Name="btn_ImageSelection"
                Content="{Binding UserFeedback}"
                Style="{StaticResource OverlayButton}" />
        </Grid>
    </Border>
</UserControl>